# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

Pizarra is a canvas-based wireframing/sketching web app with a hand-drawn aesthetic. Vanilla HTML/JS with **zero runtime dependencies** — no bundler, no framework; `index.html` loads plain `<script>` tags and a plain stylesheet. Styles are authored in **SCSS (BEM)** under `src/scss/` and compiled by **Gulp 5 + dart-sass** into `css/styles.css`, which is **committed** so cloning and opening `index.html` keeps working with nothing installed (see "Styles" below). UI text is in Spanish.

The app was formerly called SketchWire, so that name survives in identifiers that must not change: the `localStorage` keys `sketchwire.autosave`/`sketchwire.prefs`/`sketchwire.tabs`/`sketchwire.doc.<id>` and the clipboard marker `sketchwire/elements` in app.js. **Renaming those would orphan every user's saved canvas and preferences** — leave them alone.

**Document tabs (v3.17.0).** Several boards open at once; the storage contract is half the feature. `sketchwire.autosave` is ALWAYS the ACTIVE document (same `{elements, settings}` shape, with the canvas aspect now inside `settings`) — the test harness, the e2e helpers and pyzarra's disk mirror all observe the app through that key, so the active doc must never move elsewhere. `sketchwire.tabs` is the index `{v:1, active, order:[{id,label}]}` — `label` is ONLY the user's short text ('' if none); the visible name is COMPUTED as «Pizarra N - label» with N = position in the bar (v3.18.0: closing a middle tab renumbers for free; a v3.17.0 index with `name` migrates on load — factory «Pizarra N» → empty label, anything else → label); `sketchwire.doc.<id>` holds only the SLEEPING documents (the active one is not duplicated there — quota). Switching tabs stashes the active doc into its `doc.<id>`, applies the incoming one and deletes its key. No index ⇒ legacy migration: one «Pizarra 1» tab over the existing autosave, zero bytes moved. Undo/redo and camera (`zoom`, `zoomManual`, scroll) are per-tab but memory-only (`tabRuntime`). The canvas aspect is per-DOCUMENT (travels in the doc; prefs keeps the default new tabs are born with), «Limpiar todo» clears only the active tab, «Abrir proyecto» opens into a NEW tab (its `confirm()` is gone — and no new `confirm()`/`prompt()` ever: they freeze WKWebView; closing an occupied tab uses the `#modal-tab-close` dialog). `pruneImageCache` must union the srcs of ALL tabs (`inactiveDocSrcs`) or sleeping tabs lose their images. Shortcuts use Alt on purpose (`Ctrl/Cmd+Alt+T/W/←/→`): plain `Cmd+T`/`Cmd+W` belong to the browser/native window. Guards live in `tests/doctabs.test.js` and `e2e/tabs.spec.js`.

**Este fichero tiene un límite duro de 150k caracteres** (el harness lo trunca por encima, y lo que se corta se pierde en silencio). Por eso las secciones largas viven íntegras en `docs/*.md` y aquí queda un resumen con las invariantes críticas y el puntero — **lee el doc enlazado antes de tocar su área**, el resumen no sustituye al detalle. La documentación nueva de una funcionalidad sigue el mismo patrón: el detalle a `docs/`, aquí solo lo que un agente necesita para no romper nada sin darse cuenta. Comprueba el tamaño con `wc -c CLAUDE.md` al añadir secciones.

## Running

Open `index.html` directly in a browser, or serve statically:

```bash
python3 -m http.server 8000   # then open http://localhost:8000
```

Style development needs `npm install` once, then: `npm run watch:css` (recompile on save), `npm run build:css` (one-shot), `npm run build` (compile + regenerate `dist/`, the publishable build: index.html and LICENSE verbatim, CSS compressed, JS minified — gitignored), `npm run lint:css` (stylelint). Tests use Node's built-in runner (no dependencies):

```bash
node --test tests/*.test.js          # full suite
node --test tests/exporter.test.js   # single file
```

**The app itself still has zero runtime dependencies** — `index.html` loads plain `<script>` tags and the already-compiled `css/styles.css`. `package.json`/`node_modules` exist only for development: the end-to-end suite and the style pipeline (Gulp/Sass/stylelint); never make the app require them at runtime.

Use the glob (`tests/*.test.js`), not the bare directory: `node --test tests/`
silently skips a file under Node 22.x's directory globbing, so a regression guard
could pass unnoticed by not running at all.

Tests load the global-scope scripts via `node:vm` with canvas/DOM stubs (see `tests/helpers/`). Two vm-realm gotchas: arrays/errors created inside the vm don't share host prototypes, so compare structurally (`[...arr]`, `err.name`) instead of `deepStrictEqual` against host literals or `instanceof`.

**`src/js/app.js` is testable too** (since v1.13.1) via `tests/helpers/load-app.js`, which runs the whole app under `node:vm`: `dom-stub.js` builds the element tree by parsing the real `index.html` (so ids, classes and nesting match the page), and the test drives real gestures — `selectTool`, `drag`, `click`, `key` — then reads the result with `elements()`. There is **no test hook in production code**: observability comes from the autosave, which serializes `state.elements` to localStorage. `flush()` drains the queued `requestAnimationFrame`/`setTimeout` callbacks so each gesture settles deterministically. Two things to know when writing these tests: pointer-move events must carry `buttons: 1` (app.js requires it to treat a move as a drag), and prefer asserting on element coordinates/counts rather than on canvas draw calls. **The harness does not simulate browser default actions** — it caught none of the two bugs found by opening the app in a real browser (a tool shortcut leaking into the modal it opened, and the eraser's reach feeling far too aggressive). It proves logic, not feel: still open the app for anything about focus, defaults or how a gesture reads. See `tests/app-interaction.test.js`. Prefer converting a "manual repro" BUGS.md entry into one of these tests over repeating the steps by hand.

### End-to-end suite (`e2e/`, Playwright)

```bash
npm run e2e:install     # una vez: descarga Chromium (~95 MB)
npm run test:e2e        # suite e2e
npm run test:all        # node:vm + e2e
```

It **complements** the `node:vm` harness, it does not replace it. The split is the point: `tests/` proves logic (coordinates, undo, export, geometry) in milliseconds; `e2e/` proves the things the vm harness cannot see *by construction* — layout, CSS, real focus and browser default actions. That's exactly the gap CLAUDE.md already warned about ("still open the app for anything about focus, defaults or how a gesture reads"), so most `e2e/` specs are BUGS.md entries that used to say *"verificación manual"*. **Decide where a new test goes by that rule**, not by convenience.

`playwright.config.js` serves the app with `python3 -m http.server` on port 8123 (no build, same as production) and runs Chromium only. Three things bite in this app specifically, all absorbed by `e2e/helpers.js` — use the helpers rather than driving the page directly:

- **Nothing is observable synchronously.** `redraw()` is coalesced through `requestAnimationFrame` and the autosave is debounced 500 ms, so a read right after a gesture returns the *previous* state. `settle()` (two rAFs) runs at the end of every gesture helper, and `elements()` then waits for the autosave to agree with the panel's «Elementos» counter. Without `settle()` that agreement is satisfied by the stale pair (0 === 0) and the wait returns instantly with the old scene — a silent false pass, not a failure.
- **The canvas is scaled and the scaling is animated.** `canvasPoint()` maps canvas coords to viewport coords through the canvas's own rendered box, so it absorbs zoom and scroll. But `.canvas-area__sizer` transitions `width`/`height` over 0.2 s: measuring mid-transition sends clicks to the wrong canvas coordinate and reports a half-sized scroll area. Always change zoom with `setZoom()`, which waits for the box to reach its final size.
- **Window size decides the initial zoom**, because of the auto-fit. Use the measured `WIDE` (1920×1200 → 120 %) and `NARROW` (1160×700 → 100 %) constants. `NARROW` is deliberately above 1100 px: below that the panel becomes a hidden drawer and its buttons can't be clicked without opening it first.

Observability follows the same rule as the vm harness: **no test hooks in production code** — the scene is read from the autosave, and everything else from the DOM the user sees.

`PLAN.md` holds the prioritized improvement roadmap (fases 1-3 completed, except §3.2 element-type registry, deliberately skipped as optional). `BUGS.md` is the regression registry: every bug fix gets a symptom/cause/fix entry there plus a **regression guard** — a test in `tests/` for code in one of the `node:vm`-loadable modules (config/sketchy/arc/renderer/exporter/templates), or documented manual repro steps if the fix lives in `app.js` (DOM-only, outside the test harness — see below). Add the entry as part of the fix, not as a follow-up.

## Styles (`src/scss/` → `css/styles.css`)

Since v1.25.0 the stylesheet is compiled: **`css/styles.css` is a generated artifact — edit `src/scss/` and run `npm run build:css`, never the CSS itself** (a guard in `tests/smoke.test.js` checks the banner and the runtime contracts). The artifact stays committed, at that exact path, because `tests/smoke.test.js` anchors `css/styles.css?v=…`, Playwright serves the repo root with no build step, and the README promises clone-and-open. `gulpfile.js` uses the modern `sass.compile()` API directly (no gulp-sass plugin), `style: 'expanded'`, no sourcemaps, no autoprefixer — the output is meant to be deterministic and reviewable. `src/` holds the app source: `src/scss/` (compiled), `src/js/` (loaded as-is — `index.html` points its `<script>` tags at `src/js/*.js`) and `src/img/` (every image in the repo). `index.html` itself stays at the repo root because the vm harness (`load-app.js`), Playwright and clone-and-open all anchor it there.

`npm run build` additionally assembles `dist/`, the publishable build (gitignored): LICENSE verbatim, index.html with its `src/js/` and `src/img/` paths flattened to `js/` and `img/` (the only HTML transformation anywhere), the same SCSS recompiled with `style: 'compressed'`, and each `src/js/*.js` minified with **terser** into `dist/js/` keeping its filename. `site.webmanifest` gets the same `src/img/` → `img/` rewrite, because its icon paths resolve relative to the manifest itself. Terser must never mangle top-level bindings — each `<script>` exposes globals (`TOOLS`, `Sketchy`, …) as top-level `const` that later scripts consume; the default `mangle: true` (without `toplevel`) is what keeps that safe. The `src/js/` files themselves are never transformed in the source tree.

**`dist/` does not regenerate itself, and a stale one is invisible.** It is gitignored, so it can sit at an old version indefinitely while the source moves on — and an app opened from `dist/index.html` then looks and works perfectly, just *as that older version*: a tool added since is simply absent from the sidebar, with nothing on screen to hint why (v2.23.0 lost an afternoon to exactly this, see BUGS.md). `tests/smoke.test.js` now checks that **if** `dist/` exists its `?v=` and topbar badge match `package.json`, and stays quiet when it doesn't. When verifying a change in a browser and the change "isn't there", check **which path the app is being opened from** — repo root, `dist/`, or `file://` — before trusting anything else.

## Images (`src/img/`) y reglas del CSS compilado

**Detalle completo: `docs/imagenes-y-estilos.md` — léelo antes de tocar iconos, fuentes, tokens SCSS o breakpoints.** Lo que más se pisa:

- Toda imagen vive en `src/img/`, con rutas **relativas** en `index.html` (clone-and-open sobre `file://`). El set de iconos se regenera desde `icon-source-512.png` con ImageMagick; `IMG_SKIP` (gulpfile) excluye de `dist/` la fuente y todo `screenshot-*` — renombrar una captura la mete en el build. `tests/smoke.test.js` guarda existencia y `sizes` de cada icono (un 404 ahí es silencioso).
- **`1rem` = 10px** (`html { font-size: 62.5% }`); breakpoints en **px** a propósito, igual que todo lo que JS pone en el canvas.
- **Custom properties que deben seguir siéndolo en el CSS compilado**: `--sidebar-w` (se redefine en runtime) y `--font-sketch` (config.js la lee al arrancar). Los valores de diseño viven como `$vars` en `abstracts/_variables.scss` y `base/_tokens.scss` los interpola en `:root` (vía `meta.inspect()` para conservar comillas).
- **Fuentes**: catálogo en `SKETCH_FONTS` (config.js) — 7 familias autoalojadas en `fonts/` (woff2); cada entrada necesita su `@font-face` en `_font-faces.scss` y su fichero en disco, o el lienzo cae al fallback del sistema **sin error**. La app no pide ninguna fuente por red. Dibuja siempre `sketchFont()`, nunca `SKETCHY_FONT`; `setSketchFont` reescribe también `--font-sketch` (el editor flotante es un `<textarea>` real); `ensureSketchFontLoaded` fuerza la carga (un `<canvas>` no dispara webfonts). Los exports SÍ enlazan Google Fonts (`FONT_URL()`; `google: false` ⇒ se omite el link — OpenDyslexic). El selector existe dos veces (`SKETCH_FONT_SELECTS`) vía `applySketchFont`. La UI va en **OpenDyslexic** (elección de accesibilidad del usuario — no cambiarla); los botones del sidebar están dimensionados para ella (nunca `overflow-wrap: anywhere` ni `height` fija ahí).
- **El orden de `@use` en `main.scss` ES la cascada** — no alfabetizar. Selectores de vendor (`::-webkit-slider-thumb`/`::-moz-range-thumb`) jamás juntos en un grupo con coma (mixin `range-thumb`); un comentario entre reglas anidadas rompe el anidado en dart-sass (por eso los parciales densos en comentarios usan selectores planos).
- Breakpoints con nombre solo en `abstracts/_breakpoints.scss` (1201/1100/1060/420/360). La topbar pasa a iconos bajo 1060px (`$topbar-icons`): cada rótulo de botón va envuelto en `.btn__label` (guardado en smoke, que además exige `title` en todos) y el recorte es **en flujo**, jamás `position: absolute` (un absoluto escapa del `overflow: hidden` y estira el scroll de la página).
- stylelint fija la notación legacy `rgba(…)` a propósito — no «limpiar» esa config sin re-verificar el artefacto. Smoke guarda que toda custom property usada en el CSS compilado está definida.

### La Ayuda: buscador, índice y prosa (v3.3.0)

`#modal-help` son ya veinte secciones, y encontrar algo concreto era
desplazarse leyendo. Tres añadidos, con una regla común: **todo se construye
desde el propio HTML de la ayuda**, así que una sección nueva entra sola —
`buildHelpIndex()` saca las pastillas de los `<h4>` y les pone `id` a los
grupos si no lo traen, y `filterHelp(q)` recorre esos mismos grupos. Cinco
cosas que no se deducen del código:

- **`filterHelp` usa `querySelectorAll('li')`, no `.modal__help-list li`**: el
  arnés `node:vm` resuelve selectores simples, pero no descendientes, y con el
  compuesto el filtro no encontraba ni una línea ahí dentro — funcionaba en el
  navegador y ninguna guarda podía verlo.
- **El emparejamiento SOLO se puede guardar en e2e**: `dom-stub` no acumula el
  texto de los hijos, así que el `textContent` de una línea con `<kbd>`/
  `<strong>` llega vacío. En `tests/` se comprueba la máquina de estados
  (recuento, aviso de cero resultados, índice retirado, vaciar lo devuelve
  todo); en `e2e/help.spec.js`, que «boton» encuentra «Botón».
- **Coincidir con el TÍTULO trae la sección entera.** Quien escribe «jardín»
  quiere esa sección, no las tres líneas que además repiten la palabra. Ojo al
  escribir specs: «Jardín» aparece también dentro de Edificios, así que hay
  que localizar el grupo por su `.modal__help-title`, no por `hasText`.
- **El campo es `type="text"` a propósito** (`_uppercase.scss` rescata
  `input[type="text"]`, y un buscador es texto del usuario), y el buscador es
  `position: sticky; top: 0` por el mismo motivo por el que «Cerrar» lo es
  abajo: el diálogo es más alto que la ventana. Necesita la misma máscara de
  sombra sin desenfoque para tapar los 2.8rem de relleno superior.
- **`.modal__help-count`, `.modal__help-index` y `.modal__help-empty` declaran
  `display`**, así que llevan su propia regla `[hidden]` — la trampa que
  documenta `_panel.scss`, por séptima vez.

**Y la prosa de la ayuda deja de ir en mayúsculas** (`.modal__help-list`,
`.modal__help-empty` y las pastillas del índice): un rótulo corto en caja alta
se lee de un vistazo, pero frases enteras se leen letra a letra. Los `<h4>`
siguen en mayúsculas por su propia regla, que es lo que ancla la ayuda al
resto de la interfaz. La pastilla es un `<button>` y **no hereda**, así que el
rescate la nombra a través de su contenedor (`.modal__help-index button`):
apuntar a `.modal__help-chip` —una clase que crea app.js— habría fallado la
guarda que exige que cada clase nombrada en ese partial exista en el HTML.

### Tokens sin números mágicos (v3.3.1)

`src/scss/abstracts/_variables.scss` es ahora la fuente única de **toda**
medida repetida, acoplada o con significado de diseño — no solo la paleta y
las cuatro dimensiones que ya vivían ahí. El criterio, escrito en la cabecera
del propio fichero: se nombra lo repetido, lo acoplado y lo que codifica una
decisión; un padding/gap/font-size de un solo uso sin significado transversal
**no** se nombra — cuarenta variables de un solo uso serían ruido, no
documentación. Los nuevos tokens son **solo `$vars` Sass**, nunca custom
properties: nada de lo añadido se necesita en runtime, así que
`base/_tokens.scss` no se tocó y las dos reglas que exigen que ciertos valores
sigan siendo custom properties (`--sidebar-w`, `--font-sketch`, ver arriba)
quedan intactas.

Todo el refactor se hizo bajo una sola disciplina: **el CSS compilado tiene
que salir byte a byte idéntico**, comprobado con `shasum` tras cada tanda de
sustituciones. dart-sass emite `rgba($var, α)` en la misma notación legacy que
ya estaba en los fuentes y que exige stylelint, así que casi todo el trabajo
fue una sustitución de identidad garantizada. Cinco grupos de tokens y por qué
cada uno merecía nombre:

- **Derivados translúcidos de la marca** (`$primary-tint`/`-strong`/`-ring`):
  el color de marca aparecía descompuesto a mano como `rgba(78, 205, 196, α)`
  en cuatro parciales — si `$color-primary` cambiara, esas copias no lo
  habrían seguido.
- **Los tres rojos del proyecto, nombrados por separado a propósito**:
  `$color-danger` (el texto AA, v2.2.0), `$danger-bg-base` — el
  **antiguo** `$color-danger` (`#e94560`), que los fondos del botón
  destructivo conservan a propósito porque el aclarado a `#f4778c` fue solo
  del texto — y `$color-warn` (`#autosave-warn`). Nombrarlos por separado deja
  constancia de que **no son el mismo rojo** y de que unificarlos sería una
  decisión aparte, no una limpieza.
- **`$modal-pad`/`$modal-gutter`**: dos acoplamientos que antes vivían solo en
  comentarios. Las sombras-máscara sticky de `.modal__cancel` y
  `.modal__help-search` miden `± $modal-pad` porque **tienen que tapar
  exactamente el relleno del diálogo** — si `$modal-pad` cambia sin tocar las
  sombras, vuelve a asomar el hueco que documenta el comentario de
  `.modal__cancel`. Iguales de acoplados: `mx.modal-width($max)` (mixin nuevo
  en `abstracts/_mixins.scss`) reemplaza las cuatro repeticiones manuales de
  `min(Xrem, calc(100vw - 2.4rem))`.
- **`$ease-slow`/`$dur-slow`**: en `_panel-drawer.scss` la duración de la
  transición y el delay de `visibility` tienen que coincidir exactamente —
  antes eran dos literales `0.2s` que nada obligaba a moverse juntos. Ahora
  comparten variable. `e2e/helpers.js` sigue esperando esos 0.2s en el sizer
  del lienzo (`setZoom`); no tocar la duración sin revisar ese helper.
- **Escala de radios** (`$radius-sm` a `$radius-2xl`), z-index con nombre
  (`$z-canvas-overlay`/`-drawer-backdrop`/`-drawer`), `$swatch-size`,
  `$sidebar-btn-w` y `$tracking-wide`: cierran la lista de valores que se
  repetían sin una fuente común.

El refactor descubrió **dos únicas desviaciones reales** de esas escalas —un
`border-radius: 0.7rem` en la ficha de dimensiones botánicas y un
`outline: 0.15rem` en la herramienta activa del sidebar, ambos a un píxel de
la escala vigente (0.8rem y 0.2rem)— y son la única parte del cambio que
**sí** movió bytes en el CSS compilado, corregidas con el visto bueno
explícito del usuario. Ver la entrada correspondiente en `BUGS.md` y la guarda
*"las escalas tokenizadas no tienen desviaciones en el CSS compilado"* en
`tests/smoke.test.js`, que rechaza esos dos valores exactos.

### La interfaz va en MAYÚSCULAS (v2.29.0)

`src/scss/base/_uppercase.scss` pone en mayúsculas toda la interfaz. Se hace con **`text-transform`, nunca cambiando el texto del HTML**, y eso es lo que lo vuelve inocuo: `textContent` sigue diciendo «Cerrar», así que las guardas que leen el DOM (los `<legend>` de los modales, los rótulos del sidebar), las que leen `index.html` y cualquier comparación de cadenas de la app siguen valiendo — y revertirlo es borrar un fichero. Tres cosas que hay que saber:

- **Los controles de formulario NO heredan.** El navegador fija `text-transform: none` a `button`, `select`, `option` e `input` en su propia hoja, así que sin nombrarlos explícitamente se quedaban fuera todos los botones (topbar, «Limpiar todo», el «Cerrar» de cada diálogo) y todos los `<select>`: la mitad de la interfaz. Por eso el partial repite los cuatro contenedores con `button, select, option, label`.
- **Lo que escribe el usuario no es interfaz**: el rótulo de los componentes UI (`.panel__text`, `#ui-modal-label`), el editor flotante del lienzo y los campos numéricos se rescatan con `text-transform: none`. Verlo en mayúsculas mientras el elemento guarda otra cosa es un mando que miente. Una regla directa gana siempre a la herencia, así que da igual dónde caiga en la cascada.
- **Rompió el sidebar y hubo que recalibrarlo.** «RECTÁNGULO», «REDONDEADO», «AROMÁTICAS» y «TREPADORAS» ya no cabían en la columna estrecha del modo de dos columnas y se salían por los lados tocando al botón vecino; el cuerpo bajó a `0.8rem` con `letter-spacing: -0.02em` **sólo en ese modo**. La guarda de `e2e/responsive.spec.js` no lo veía: medía `scrollWidth > clientWidth`, y el rótulo crece a `max-content` con overflow visible, así que su caja nunca "desborda" — ahora compara además el ancho del rótulo con el del **botón**.

## Architecture

Scripts are plain `<script>` tags loaded in dependency order in `index.html` (config → sketchy → freehand → arc → curve-path → shape-rotation → regular-polygon → trapezoid → hatch → airbrush → flood → eraser → building → garden → solid → renderer → exporter → templates → app). There are no modules/imports — each file exposes a global (`TOOLS`, `Sketchy`, `Freehand`, `ArcMath`, `CurvePath`, `ShapeRotation`, `RegularPolygon`, `Trapezoid`, `Hatch`, `Airbrush`, `Flood`, `Eraser`, `Building`, `Garden`, `Solid`, `Renderer`, `Exporter`, `Templates`) via IIFE, and later scripts rely on earlier globals. If you add a file, add its `<script>` tag in the right position (and, for the test harness, in `ALL_FILES`/`KNOWN_GLOBALS` of `tests/helpers/load.js`).

The app is state-driven immediate-mode rendering: a single `state.elements` array of plain objects is the source of truth, and any change triggers a full canvas redraw (`redraw()` in app.js). Elements are serializable plain objects (this is what JSON export/import round-trips), so never store functions or DOM refs on them. They are also treated as immutable: operations replace the element object (`moveElement` returns a copy) rather than mutating it, which lets undo snapshots be shallow copies (`state.elements.slice()`) — preserve that discipline when adding features.

- `src/js/config.js` — global constants: `TOOLS` ids, `TOOL_GROUPS` (drives sidebar build), `COLORS`, `CANVAS_W/H` (1200×800), `UI_DEFAULTS` (fallback sizes for UI components placed with a tiny drag). **`COLORS` is laid out, not just listed**: `.panel__color-grid` is `repeat(6, 1fr)`, so each run of six in the array is one visible row, and since v2.19.0 the 36 colours go tinta/neutros (dark→light, opening with the default `#1a1a2e`), then the vivos and the pasteles each sweeping the rainbow by ascending HSL hue, darkest first within one hue. A guard in `tests/config-templates.test.js` computes the hue and fails if a colour goes in out of order, if the total stops being a multiple of six, or if a vivo lands among the pasteles — and any e2e that clicks a swatch must address it by `[data-color="…"]`, never by index.
- `src/js/sketchy.js` — low-level "wobbly" hand-drawn primitives (line, rect, roundedRect, ellipse, arrow) using `Math.random()` jitter.
- `src/js/arc.js` — pure circular-arc math (`ArcMath`): fits a single cubic Bézier to an arc of a given sagitta over a chord (≤ 180°). A `curveArrow` with `arc: true` is a normal cubic whose controls were computed this way, so renderer/exporters/hit-test need no arc-specific code. The `TOOLS.ARC` ("Semicírculo") and `TOOLS.ARC_ARROW` ("Flecha semicírculo", v3.20.0) sidebar tools are creation-mode only — they are **not** element types; both create `curveArrow` elements that are always exact 180° semicircles (drag = diameter). The only difference is the head: `ARC` stamps `heads:'none'` (a stroke, not a connector) while `ARC_ARROW` leaves `heads` absent (≡ head at the drag's end) and stamps `headShape`/`doubleHead` like the other arrows — renderer, exporters, hit-test, eraser and validation needed zero new code because the head is painted from the curve's end tangent. Both ids live in `CREATION_ONLY_TOOLS` (exporter.js), in `STROKE_TOOLS`, and map to `curveArrow` in `MODAL_EDIT_TYPE`; the head/dash predicates in `syncStrokeControls`/`syncPanelSections` must name `ARC_ARROW` explicitly or its «Punta» controls grey out with the tool active. Editing keeps the 180° invariant: `+/−` and the ctrl-handle drag change the radius via `resizeArc` (app.js), and endpoint drags preserve the shape through `transformControlsToChord`.
- `src/js/curve-path.js` — pure shared geometry for `curveArrow`, including chained curves. Historical single curves keep `x1/y1/cx/cy[/cx2/cy2]/x2/y2`; a chain also carries `segments`, an ordered, continuous array of quadratic/cubic Bézier objects, while top-level `x1/y1/x2/y2` mirror only the overall endpoints for connector anchoring. Use `CurvePath` for rendering, bounds, sampling, moving, scaling, reversing and endpoint/join editing so those mirrors never drift.

  **La comba de creación es un parámetro desde la v3.2.0** (`state.curveBulge`, fracción de la cuerda; `CurvePath.DEFAULT_BULGE` = 0.25 es la de siempre). Cuatro cosas que la sostienen:

  - **El elemento no estrena campo**: la curvatura ya vive en `cx/cy`, así que renderer, exportadores, `isValidElement`, borrador y bounds no se tocan y un proyecto anterior se dibuja igual. Es la economía del aerógrafo: el ajuste es de la herramienta, no del dibujo.
  - **`CurvePath.defaultCtrl(p1, p2, flip, bulge)` lleva el valor por defecto en la firma**, y eso es lo que impide que un llamador que no se enteró del parámetro cambie de forma en silencio. Un `bulge` no finito cae también en el de fábrica: propagar `NaN` a `cx/cy` deja la curva sin dibujar y sin error.
  - **Todo lo que CREA una curva pasa por el `defaultCtrl` local de app.js** (commit, previsualización del arrastre, cada tramo del modo encadenado, el reset por doble clic, la S) — o la muestra prometería una comba y el lienzo dibujaría otra. **La excepción es `transformControlsToChord`**, que llama a `CurvePath.defaultCtrl` **sin comba**: allí se repara un elemento existente de cuerda degenerada, y con el ajuste del momento arrastrar un extremo le cambiaría la forma a una curva ajena.
  - **El mando (`#stroke-modal-curve`) sólo edita curvas SIMPLES**: en una cadena la comba es de cada tramo y en un `arc` es el radio con su invariante de 180°, así que se atenúa cuando la selección no tiene ninguna — no dibujar promesas que el gesto no cumple. Edita fijando la magnitud lateral de cada control y **conservando su signo**, que es lo que preserva el lado del giro y la forma en S.
- `src/js/shape-rotation.js` — pure discrete rotation rules. `rotateElement(el, dir)` takes a direction (1 clockwise, the historical default and what `Shift+R` uses; -1 counter-clockwise, added in v2.23.0 for the `←`/`→` shortcut). **The two directions must stay exact inverses** — rotating and un-rotating has to give back the same object, `rotation` field *absent* rather than a stored 0, or a round trip would serialize differently from what was rotated (guarded for all nine types). `ShapeRotation.rotateElement` swaps a rectangle's `w/h` around its unchanged center for each 90° turn, and normalizes the `rotation` field of triangles/pentagons/hexagons/stars after 90°/36°/30° turns. A star's step is its polygon's, not `360/2n`: half a step lands on the same silhouette rotated onto itself. Unsupported types are returned unchanged.
- `src/js/regular-polygon.js` — pure geometry for `triangle`, `pentagon`, `hexagon` **and the two stars**: side counts, vertices, center-radius creation and real-silhouette containment. These elements use the standard `x/y/w/h` shape schema but must keep `w === h`; app resize enforces the square and import validation rejects deformed polygons.

  **The stars live here, not in a module of their own, and that is the whole design.** `star5`/`star6` **are** element types (unlike `arc`, Emoji or the Edificios/Jardín tools), and their silhouette is just one more polygon — concave, with **twice as many vertices as points** (10 and 12), alternating outer and inner radius. Sharing `vertices()` is what makes them inherit, without a line of their own, the hit-test, the eraser's real-silhouette reach, fill, «Bordes ocultos», the discrete rotation, the mandatory square box and both vector exporters. Four things to keep:

  - **The inner radius is canonical, not a taste knob**: `innerRatio(n) = cos(2π/n) / cos(π/n)` (0.381966 for 5, 0.577350 for 6) is the one where **extending a point's side lands exactly on another point**, so the outline is made of whole straight lines instead of a flower of straight petals. That is what gives the classic pentagram and Star of David; any other ratio draws a fatter or scrawnier star. Guarded by *"El radio interior es el canónico: el lado de una punta apunta a otra punta"*.
  - **The box stays the circumscribed circle's**, so a star's real bbox doesn't fill its square on the sides. Accepted deliberately: it's what keeps rotation, scaling and `w === h` behaving exactly like every other regular polygon.
  - **Even index = outer radius**, so a star is born point-up like the polygons are born vertex-up (`startAngle = -π/2`).
  - **Every type list that names the polygons must name the stars too** — `REGULAR_POLYGON_TYPES`/`SHAPE_TOOLS` (app.js), `OUTLINE_TYPES` (eraser.js), `OVERLAP_SHAPE_TYPES` (renderer.js), `VECTOR_TYPES` and the SVG `<polygon>` case (exporter.js). Missing one fails silently and differently in each: the eraser would use the bounding box, the export would drop the shape, «Bordes ocultos» would ignore it. They are pinned by *"Renderer, borrador y solapamiento tratan la estrella como una forma más"* and *"Exporter valida, exporta y reimporta las estrellas"*.

  They carry **no keyboard shortcut** — the 26 letters and 10 digits are taken, and `f q d s` are curve-arrow actions checked before `TOOL_KEYS` — same as Balcón, «Select» and the Aerógrafo; the exact keyless list is pinned in `tests/config-templates.test.js`.
- `src/js/trapezoid.js` — pure geometry and quarter-turn rotation of the two «box shapes»: the isosceles `trapezoid` and, since v3.19.0, the `freeTriangle` (standard `x/y/w/h` schema, free proportions). The irregular triangle stores its top vertex in `apex` (fraction of the width, open (0,1); ABSENT means isosceles 0.5 — the compact format — and `Trapezoid.APEX` maps the two modal choices: isosceles 0.5, escaleno 0.25). Everywhere the code used to ask `type === 'trapezoid'` it now asks `Trapezoid.isType(type)` — renderer contains/outline, eraser deps, exporter fill/validation, app hit-test, mirror and quarter-turn — so both shapes share one circuit; the `#modal-shape` row `#shape-modal-tri-row` (dual semantics like rotation) only shows for the freeTriangle, and **mirroring flips the vertex** (`apex → 1 - apex`, both axes) because the triangle is not symmetric. `isValidElement` rejects `apex` on any other type.
- `src/js/building.js` — pure geometry (`Building`) for the **Edificios** section (see below). `Building.elements(tool, p1, p2, opts)` returns an array of plain **existing-type** elements (`rect`/`line`/`circle`/`curveArrow`), so renderer/exporter/`isValidElement`/bounds need no building-specific code. Covers balcones (railing + cantilevered slab in elevation, 8 types via `opts.balconyType`; `_railing` takes a `bar` callback so the forja can bow its bars with `ArcMath`, and the balcony is the only tool whose `DEFAULTS` entry carries `variantKey`/`byVariant`), plantas (rect/L/U/claustro), fachada in three vistas (flat/alzado/perfil via `opts.facadeShape`; multi-floor, windows + door, tiled roofs), tejados (dos aguas/un agua/plano/**cuatro aguas**/**mansarda** via `opts.roofShape`), and the Puerta types (door/arch/double/panel/garage + frame/archFrame/doubleFrame marcos) and Ventana types (window/arch/double/grid/round + frame/archFrame/roundFrame marcos). `_openings` **honours `opts.doorType`/`opts.windowType`** (facades draw the rich hole types via `_doorTool`/`_windowTool`, degrading to the basic hole below a size threshold), and `opts.floors`/`opts.bays`/`opts.roofPitch`/`opts.roofType` override the drag-derived plantas/vanos/pitch/roof (absent or `'auto'` = legacy behaviour). Door and window arches share one `_arched(b,o,{frameOnly,cross})`; the sill is `_sill`; the cornice is drawn only on the roofless facade (`_body(...,crown)`). The **perfil is a side view, not a second facade**: `_profile` passes `side=true` through `_body` to `_openings`, which forces `ground=false` so the lateral gets **no door** and an even bay rhythm on every floor (no central gap reserved for an entrance) — keep that flag if you touch `_openings`. Detail (mullions, sills, tiles, panels, slats, grid bars) uses a thinner `lineWidth` than the contour (`_lineT`/`_rectT`); arches are `ArcMath` `curveArrow`s with `arc:true, heads:'none'` and the óculo is a `circle` element. Elements come out **without `seed`** (app.js adds it via `withSeeds`); app.js also stamps a shared `buildingGroupId` on every piece of a multi-piece building.
  **Muros, Verjas y Cancelas también viven aquí.** Muro admite piedra,
  hormigón o ladrillo cara vista, verja superior y 18 entradas. Verjas comparte
  13 paños de forja entre su herramienta autónoma y la coronación del muro;
  `_forgeFinial` da a cada uno una hoja clásica distinta. Cancela reutiliza las
  mismas hojas y pilastras mediante `GATE_TYPES`, pero construye un vano
  autónomo en planta o alzado. Verjas y Cancela fijan el ancho con el arrastre
  y el alto con sus cotas independientes de 0–350 cm; a cero emiten únicamente
  la línea de implantación.
- `src/js/airbrush.js` — pure geometry (`Airbrush`) for the **Aerógrafo**'s cloud of dots. Unlike `arc`/`building`/`garden`, `airbrush` **is** an element type (`type:'airbrush'`, in `TOOLS`, in the «Dibujo» group, **no shortcut** — no plain key is left). The element stores the **axis** (`points`, decimated at 2 px like the pencil) plus `radius` (nozzle), `density`, `lineWidth` (grain) and `seed`; the dots are regenerated here every time they're needed — canvas, all five exports, the modal thumbnail and the drag preview — so the JSON stays small and the five outputs cannot diverge. Seven rules, each of which broke or would have broken something real:
  - **The randomness is a FUNCTION, not a generator.** `_rnd(seed, seg, dot, channel)` is a stateless hash. `mulberry32` (sketchy.js) is a sequential stream, and that is exactly the *boiling* problem: change the number of dots in one span and the whole sequence shifts, so the cloud re-rolls on every frame of a resize — and on every point added while drawing. With a per-dot hash, growing the stroke only **adds**, and scaling moves the survivors affinely (guarded by two tests, each verified against its own mutation).
  - **`t` comes out of the hash, never from `i/n`.** That is what makes the surviving dots keep their position when the dot count of their span changes. The **deterministic dithering** (`_rnd(...,7) < fraction`) is what lets 2 px spans not force ≥1 dot each and blow up the real density of thin strokes.
  - **The radial spread is 2-D isotropic** (`rho = R·u^SPREAD`, uniform angle), not a perpendicular offset. A 1-D `R·rand**k` is singular on the axis for any `k>1` — it draws a hard bright line down the middle, the defect that makes a fake airbrush look like a highlighter — and leaves **square caps**. The 2-D version gives round caps for free (the union of discs along the polyline is a stadium; a click with no drag is a round puff) and makes **`radius` a hard bound**: no ink beyond R of the axis, which is what lets bounds, the area clip and the eraser's reach be exact.
  - **The cap is a target, not a cut.** `MAX_DOTS` degrades the *rate* through the factor `k`; a `break` would leave the end of the stroke blank. `estimate()` returns `capped` so the modal can say so instead of lying.
  - **`getElementBounds` returns the BAND** (`Airbrush.visibleBox`), the axis inflated by `radius` and intersected with `clip`. With the axis box the selection frame cut the cloud in half and the box hit-test lied. `hitTest` measures distance to the polyline for the same reason lines and arrows do.
  - **Resize is uniform**, like regular polygons and groups (`resizeTo` and `applyGeometry` both). The nozzle is one scalar; a box stretched on one axis would need an elliptical nozzle, which the model doesn't have. `radius` scales, **`lineWidth` (grain) doesn't** — its slider has a fixed 1–8 range and a resize that pushed it out of range would leave that control lying about what's drawn.
  - **The look was calibrated in a browser, not derived.** It shipped at grain 3 / density 45 and came out **speckled**, not sprayed: loose dots, no core. From grain 5 up the dense core with the faded edge appears. That is why the defaults are grain 5 / density 70 and `MAX_DOTS` is 6000 — at 3000 a long stroke at high density always hit the cap, so the mancha stopped thickening no matter what the slider said. Guarded in `tests/airbrush.test.js` (the shape properties) and `e2e/airbrush.spec.js` (real pixels).
- `src/js/eraser.js` — pure geometry (`Eraser`) for what an eraser stroke touches: `touches`/`doomedIndices`/`apply`, plus segment-segment distance with a crossing test. Everything that lives outside is injected via `deps` (`boundsOf`, `sampleCurve`, `polygonVertices`, `trapezoidVertices`) so the module stays DOM-free and testable; app.js supplies them in `eraserDeps()`. **Since v1.14.0 the eraser deletes elements instead of masking them** — the old `type:'eraser'` element was a `destination-out` mask fixed in canvas space, so moving the drawing slid it out from under the mask and the "erased" content came back (and still travelled inside exported JSON). The reach rule is **what you can see, not the bounding box**: for `OUTLINE_TYPES` shapes only the contour counts (box edges, sampled ellipse, or real polygon/trapezoid vertices) unless `el.fill` is set, in which case the interior is ink too — the interior of the **silhouette** (point-in-polygon over the same outline vertices), not of the bounding box: the bbox corner of a filled circle is ~15px away from any ink and must not erase it; text, images and UI components still use the box, because there the box *is* the drawing. Using the box for everything — the obvious "same as `hitTest`" choice — makes one sweep through the middle of a facade delete the whole wall. A generous box is right for *selecting* and far too aggressive for *erasing*. **Legacy `eraser` elements are still rendered, exported and validated** so older projects look unchanged, and `doomedIndices` skips them — deleting a mask would reveal exactly what it hides. Only the tool stopped creating them.
  **Since v1.22.0, `line`/`arrow`/`pencil` are recorded, not deleted, by `Eraser.erase(elements, pts, r, deps)`** (used by app.js instead of `doomedIndices`+filter): a touched line/arrow is resampled every `LINE_SAMPLE_STEP` (4px) and a touched pencil's own `points` are walked directly; `_survivingRuns` classifies each point as erased/kept and bisects the exact crossing at every transition, so what remains is whatever falls outside the circle, split into as many pieces as needed — this is what makes erasing through the middle of a line, or through where two strokes cross, only take the bit under the pointer instead of the whole element. An `arrow` piece only keeps its type (and head) if it contains the original tip (`x2,y2`) by reference — a run built from an interpolated crossing point never equals it — so a fragment cut away from the tip becomes a plain headless `line` rather than inventing a new arrowhead at the cut; a `heads:'both'` arrow keeps that flag only on a piece that still holds *both* original endpoints, which in practice means the split didn't really touch it. **Since v2.33.0 the same treatment reaches `curveArrow`, the `airbrush` and the outline of an unfilled shape** — before, grazing any of the three deleted it whole, which the user reported as "the eraser works with Pencil/Line/Arrow but not with the airbrush, the curved arrows or the shapes". Five rules hold that extension together:

- **A cut piece comes out as `pencil`** (`_asPencil`), built from scratch and never with `{...el}`: a `rotation` inherited by a pencil is **rejected** by `isValidElement` on import, and `w`/`h`/`fill` would be serialized garbage. `dash` is dropped too — the renderer doesn't apply it to a pencil, so copying it would draw solid while claiming dashed. `buildingGroupId` *is* kept: the fragment is still a piece of the same building/garden/solid. A curve's heads and label go with the cut, same reasoning as arrow→line.
- **A shape's outline is a closed RING, and its seam must be sewn back**: the ring is closed by repeating its first point, split, and the two runs sharing that seam are rejoined. Without it, erasing anywhere on a rectangle also broke it at the seam, into two pieces where the eraser never passed.
- **The ring and the reach must be the SAME outline** — `_shapeOutline` is shared by `touches` and `_splitOutline`, or the eraser would report touching one thing and cut another. The `roundedRect` grew a real rounded outline for this (`_roundedOutline`, at the renderer's 12 px radius); with the box outline its corners were cut square.
- **Everything is densified to `LINE_SAMPLE_STEP` before splitting** (`_densify`, keeping the original vertices *by reference* so the intact-check still works). A rectangle's outline is four points: erasing the middle of a side changes the classification of neither of its endpoints, so nothing was cut at all.
- **A FILLED shape still goes whole**: its drawing is a surface, and no element type can represent a bitten surface. The airbrush is the one that splits its **axis**, with the same `r + radius` margin as `touches` — that "full nozzle" is geometry, not caution: the surviving axis keeps spraying up to `radius` into the gap, so cutting at `r + R` leaves a clear gap of exactly `r`. With half a nozzle the measured residue in the gap was 7 % of the ink, i.e. a faint smear right where you just erased.

**Since v2.34.0 the UI section is erased by RASTER (`deps.rasterErase`, app.js)** — `text` (Texto and Emoji), `image`, `imagePlaceholder`, `button`, `input`, `nav`, `card`. There is no geometry to split there (a word is glyphs, a component is outline + label + tint, an image is a raster) and no element type can express "a button missing a corner", so the element is rendered offscreen exactly as `Renderer.renderElement` draws it, the stroke is punched out with `destination-out`, and what remains becomes an `image`. **The look is identical by construction** — it is the same drawing — at the price of no longer being editable as text or as a component. It's the same demotion ladder as arrow→line and curve→pencil, one rung further down. The user was offered the alternatives (text cut by letters + components whole; or precise reach only) and chose this. Six things it rests on:

- **It lives in app.js, not eraser.js**, because it needs a canvas and eraser.js is pure geometry. Without the dep — the `node:vm` harness, the exporters — the eraser falls back to whole-element delete, which is why *these* types can only be guarded in `e2e/eraser.spec.js`.
- **Comparing the ink before and after the punch is what gives exact reach for free**: if not a single pixel changed, the element is returned **by reference** and nothing happens. Crossing the empty middle of a card no longer deletes it, and there is no phantom undo step.
- **The preview cannot pay a `toDataURL` per frame** — and its decode is async, so the element would blink through the whole gesture. In preview mode the piece carries the live canvas in `bitmap`, which `Renderer._image` draws before looking at `src`. It never reaches `state.elements` (elements hold plain data, never DOM refs); the committed piece carries `src` instead. Same geometry both ways, so the preview matches the result exactly.
- **A half-decoded `image` renders as its dashed placeholder**, and rasterizing that would replace the photo with the placeholder. `Renderer.imageReady(src)` (which also warms the cache) gates it: not ready → return the element untouched.
- **The result is cropped to the remaining ink.** The raster is padded (Sketchy's wobble, the dashed frame, and above all the text shadow, which scales with the font size), so without cropping the new image would carry a wide empty margin and its selection box would lie.
- **A bitten PHOTO re-serializes as lossy WebP, never PNG** (v2.35.0): `toDataURL('image/png')` on photographic content multiplied its weight ×5-7, and two bitten photos blew localStorage's quota — every later autosave then failed inside a silent `catch` and work drawn afterwards was lost on reload. The choice keys on the *source* (`src` JPEG/WebP → WebP with the hole's alpha; line art — text, components — stays PNG, where it is smaller and crisper), with a prefix check because a browser without WebP support returns PNG silently; `IMAGE_SRC` (exporter.js) accepts `data:image/webp`. And the quota failure is no longer mute: `#autosave-warn` in the topbar lights while the save doesn't fit and clears itself when it fits again (`saveAutosaveNow` reflects the last attempt's real outcome).

**The whole per-frame cost lives in `eraserSession` (v2.35.0), born on the drag's first frame and thrown away on release** — nothing survives between passes, which is itself load-bearing: a WeakMap of pre-punch ink counts that outlived the pass returned stale counts after a canvas-font change, and a graze that erased *nothing* replaced the component with an image (auditoría v2.35.0). The session has two halves:

- **Geometric memos (`deps.session`, a Map keyed by element)**: the stroke only grows and an erased sample never un-erases, so `_survivingRuns` classifies each frame only against the NEW segments, `touches` retests only the stroke's new tail (with one point of overlap), and a stroke-bbox reject skips far elements before any sampling — sampled points are cached in the memo too, because the intact-by-reference checks compare run endpoints to them. Guarded by an equivalence test (incremental result ≡ full recompute) — the number (15.8 → 0.4 ms/frame; a 40-frame drag now costs what one frame used to) is a consequence, the equivalence is the correctness.
- **Raster previews (`eraserSession.raster`)**: one padded canvas per element, rendered once, punched incrementally (`destination-out` accumulates; each new segment stroked round-capped — the union equals the whole round-joined polyline). No pixel counts, no crop, no `toDataURL` in preview: the piece carries the live canvas in `bitmap` covering the whole padded box (transparent margins draw nothing). The commit (`rasterErase`, once per pass on release) computes fresh «antes»/«después» stats and crops, exactly as before.

`_erasedAt` rejects by segment bounding box before computing any distance — with the session on top, that inner clip only pays for the new tail. **Since v2.2.0 the split classifies with the same ink margin as `touches` (`r + lineWidth/2`)** — with bare `r` there was a band (between the axis and the stroke's edge) that "touched" without erasing anything visible yet still rebuilt the element; a graze that erases no sample now returns the element intact **by reference** (no phantom undo step, no silent de-anchoring), and a piece that keeps its original arrow endpoint keeps that endpoint's anchor (a piece demoted to `line` drops anchors — `resolveAnchors` ignores lines). The eraser's own size is `state.eraserSize`, edited **only** in `#modal-eraser` — `applyEraserSize()` is the single point that keeps the slider, the preview canvas and `state.eraserSize` in sync. Until v2.21.0 it had a second mando: the panel's stroke slider, *repurposed* (retitled "Tamaño del borrador", min/max swapped for `ERASER_SIZE_MIN/MAX`). **That whole slider is gone** — a control whose meaning changed with the active tool is exactly what the panel stopped doing; see "A tool's own settings…" below. **Selecting the Eraser tool opens `#modal-eraser` immediately**, same as Planta/Puerta/Balcón open their catalogue (`openEraserSizeModal()`, called from `selectTool`) — pressing the tool again reopens it later without leaving it (**since v2.21.0 that is the only way in from the panel side: «Trazo» has no ⚙**, see below). Unlike those catalogue modals, closing it does **not** revert to the tool active before: Eraser is already usable with its current size, there's nothing to "cancel" into. It is deliberately *not* added to `opensVariantModal`, for that same reason — same as Emoji.
- `src/js/garden.js` — pure geometry (`Garden`) for the **Jardín** section (see below), with plan and elevation renderers for vegetation. Same contract as `building.js`: `Garden.elements(tool, p1, p2, opts)` returns plain **existing-type** elements — `rect`/`line`/`circle`/`curveArrow`/`text` — with no `seed` and no DOM. `Garden.plantSize(tool,id,opts)` is the single botanical sizing function: adult height/spread/depth × stage factor × 50–150%, and click placement converts metres through 8–50 px/m. Detail uses `_fine(o)`, while `_chain`, `_blob` and `_rosette` carry organic plan silhouettes; elevation builders add trunks, branch architecture, layered crowns, flowers/fruit, ground lines and climbing supports. Natural mode uses per-species `foliage`/`accent` fills with transparency; ink mode stays monochrome. **The module must stay deterministic** — no `Math.random()`; irregularity comes from frozen tables and the hand-drawn wobble is added by `Sketchy` from `el.seed`. Labels can be common, botanical or dimensioned text; injected `measureText` may only move a label, never alter geometry.
- `src/js/solid.js` — pure geometry (`Solid`) for the **3D** section (see below). Same contract as `building.js`/`garden.js`: `Solid.elements(tool, p1, p2, opts)` returns plain **existing-type** elements with no `seed` and no DOM, and is deterministic (no `Math.random()`). Cavalier projection, so the back cap is the front translated by `d` — which is what lets the **front face be emitted as the real 2D shape element of its section** (`pentagon`, `circle`, `star6`…) instead of a polyline. Prism, frustum and pyramid are one parametric generator with cap factor `k` (1 / 0.1–0.95 / 0).
- `src/js/renderer.js` — `Renderer.renderElement(ctx, el)` switches on `el.type` and draws each element type (shapes via Sketchy, plus composite UI components: button, input, imagePlaceholder, nav, card). Also grid and selection highlight.
- `src/js/exporter.js` — export to PNG/JPG (offscreen canvas), SVG and HTML (hand-built markup strings per element type), and JSON (project format); JSON import via file picker.
- `src/js/templates.js` — predefined element arrays (landing, dashboard, form).
- `src/js/app.js` — main controller IIFE: holds `state`, wires all DOM events, mouse handlers (two-canvas setup: `main-canvas` for committed elements, `overlay-canvas` for in-progress previews), hit-testing/drag for the select tool, undo/redo (deep-cloned snapshots of `state.elements`), text input overlay, modals.

Two element types have no sidebar button: **`polygon`** — a free polygon defined by `points`, with its own fill and an optional contour (`stroke: false` draws only the fill). It is what a 3D solid's lateral face is: an arbitrary quadrilateral no other type could represent. It stays out of `OVERLAP_SHAPE_TYPES` on purpose (`_box` assumes x/y/w/h, and a solid's faces are precisely the ones that should read solid, not dashed), and out of `CREATION_ONLY_TOOLS` for the same reason as the airbrush — it IS an element type. And `image` (pasted via Ctrl/Cmd+V), whose `src` must be a base64 `data:image/png|jpeg` URL — enforced by `isValidElement` to keep injection out of SVG/HTML exports. Renderer keeps an image cache; `Renderer.setImageLoadCallback` is how app.js gets repaints when async loads finish.

The sidebar is responsive through CSS only: its default `--sidebar-w: 7.2rem`
(72px computed) and flex groups form one column up to 1200px. The
`@media (min-width: 1201px)` rule widens it to `13.2rem` (132px) and turns every
`.sidebar__group` into a two-column grid, with labels spanning both columns
(`src/scss/components/_sidebar.scss`). Keep that exact breakpoint aligned with the
documented desktop behaviour — e2e asserts the computed 72/132px on both sides.

**Barras flotantes (v3.13.0) — detalle completo: `docs/barras-flotantes.md`,
léelo antes de tocar `buildFloatbars`, `FLOATBAR_GROUPS` o `_floatbar.scss`.**
El botón «Barras» del topbar conmuta el sidebar por 5 paletas flotantes
arrastrables (reparto 7→5 en `FLOATBAR_GROUPS`, config.js, con guarda de
partición). Lo innegociable: los botones flotantes llevan **su propia clase
`.floatbar__tool`, jamás `.sidebar__tool`** (tres guardas cuentan con que esa
clase exista una sola vez por herramienta), y `#sidebar` conserva siempre sus
botones — el modo solo lo oculta por **CSS puro** (`app--floatbars` +
`min-width: 1101px`, el complemento exacto del cajón; el JS no mide la
ventana). El MODO persiste en prefs (`floatToolbars`); **posiciones y plegado
viven solo en el DOM y mueren con la sesión a propósito** — recargar Y activar
el modo (`resetFloatbars`, v3.13.1) es volver a fábrica. **La fábrica es FLUJO
CSS, no coordenadas** (v3.13.3): `#floatbars` es una franja fija en el borde
izquierdo (el sitio del sidebar) con `overflow-y: auto`, las barras se apilan
dentro sin huecos, y solo el arrastre pone `position: fixed` inline — resetear
es BORRAR esos estilos, nunca calcular posiciones. **Soltar una barra sobre esa
franja la acopla SOLA** (v3.14.0, `dockFloatbar` = los mismos tres borrados;
el flujo la recoloca en su orden sin medir nada, y las demás no se tocan): la
zona es la constante `FLOATBAR_DOCK_W = 150` acoplada al `width: 15rem` de
`.floatbars` —jamás su caja medida, que en el arnés vm es 1200×800 como todo—,
decide el PUNTERO y solo acopla el `pointerup`. Los botones van en dos
columnas (la rejilla del sidebar ancho) y `FLOATBAR_W` (app.js, 136) está
acoplado al `width: 13.6rem` de `.floatbar`. El arrastre por el asa es
mobiliario: jamás toca `state`, undo ni
autosave; `clampFloatbars()` corre en el resize para que ninguna barra quede
irrecuperable. Cada barra es un `role="toolbar"` con su propio roving
(`wireRovingToolbar`, compartido con el sidebar igual que `toolButton`).

One controlled exception to immutability discipline: `resolveAnchors()` in app.js runs at the start of `redrawNow` and materializes anchored arrow endpoints (`startAnchor`/`endAnchor` referencing an element `id`) back into the elements — always by replacing with copies, never with `saveUndo` (it's derived state: snapshots capture materialized coords and the post-undo redraw re-resolves). When the chord of a `curveArrow` changes, its control points are re-projected through the old-chord→new-chord similarity transform (`transformControlsToChord`) so the curve keeps its shape — the same helper runs while dragging an endpoint handle in `resizeTo`. Element `id`s are assigned lazily only when something anchors to them.

### Adding a new element type

Requires touching several files in sync. The list below is the complete one — it was written after adding `polygon` (v2.25.0), because the short version this section used to carry omitted three things that are **not** optional today, and each of them fails silently:

1. **`TOOLS`** (config.js), plus a `TOOL_GROUPS` entry **only if it gets a sidebar button** — `image` and `polygon` deliberately have none. Never add it to `CREATION_ONLY_TOOLS` (exporter.js): that is the list of tools that are *not* element types, and putting a real type there drops it from `ELEMENT_TYPES` so nothing of it survives an export→import (the trap the AIRBRUSH comment warns about).
2. **A render case in `Renderer.renderElement`.** PNG/JPG come free from there.
3. **Creation logic** in app.js `onMouseUp` (and `UI_DEFAULTS` if it's a UI component) — unless another module emits it, as `Solid` does with `polygon`.
4. **`getElementBounds`** (app.js) if it isn't x/y/w/h-shaped. Skipping this is not a cosmetic bug: the fallback returns `{x: undefined, …}` and takes down selection, the marquee, `selectionBounds`, `applyGeometry` and the eraser's `boundsOf` at once.
5. **`VECTOR_TYPES` + a `case` in `_svgElement`** (exporter.js). Being in `VECTOR_TYPES` covers **both** SVG and HTML (the type goes inside the embedded `<svg>`), so the HTML `switch` needs nothing.
6. **A branch in `isValidElement`**, placed *before* the generic final `return`, which demands x/y/w/h > 0 and would otherwise reject the element on import. Copy the `pencil`/`airbrush` pattern for point arrays.
7. **`OUTLINE_TYPES` (eraser.js) + `eraserDeps()` (app.js)**, or the eraser works by bounding box — the "one sweep through a facade deletes the whole wall" defect that `OUTLINE_TYPES` exists to prevent.
8. **Decide about `OVERLAP_SHAPE_TYPES`** (renderer.js) explicitly. `polygon` stays out on purpose: `_box(el)` there assumes x/y/w/h and would silently yield `NaN`, and a solid's faces are precisely the ones that must read solid rather than dashed.

`moveElement`, `scaleElement`, `resizeTo` and `applyGeometry` need **nothing** for a type defined by a `points` array — their `m.points` branch is already generic. `tests/helpers/` needs nothing either unless you add a whole new module file.

### Panel controls: dual semantics

Panel controls follow an Excalidraw-style dual semantic: **with a selection they edit the selected elements** (immutable copies + one undo step per gesture); **without a selection they set the creation default** (`state.lineWidth`, `state.doubleHead`, `state.fillShapes`, …). `redrawNow` is the single sync point that pushes the right value back into each control (single selection → element's value; no selection → default; multi-selection → left untouched). Any future panel control inherits this question — follow the same pattern (see the stroke slider, the "Doble punta"/"Rellenar formas" checkboxes and the fill color picker in `wireControls`). **Since v2.9.0 the stroke colour picker follows it too** — which meant nothing already drawn could ever be recoloured; `showColor()` updates the swatches and the picker without touching `state.color`, so `redrawNow` can display the selection's colour without overwriting the creation default. **Since v2.10.0 the font-size slider was the last to join** (`applyFontSize`, same gesture-snapshot undo as `applyStrokeWidth`): with a `text` selected it resizes it live, without touching `state.fontSize`.

`#panel-sec-element` ("Posición y tamaño") is the one section that is pure element editing: X/Y/width/height plus the element's own text (`value` on a `text`, `label` on the UI components — `LABEL_FIELD`). It reads through `getElementBounds` and writes through `scaleElement` + `moveElement`, the same pair the handles and the drag use, so it works for **every** type (freehand strokes and multi-selections included) without a case per type and cannot drift from what dragging does. Four things it must keep:

- **It sits FIRST in the panel**, above «Trazo». It was added at the bottom and that alone made it useless: selecting something put the fields below Lienzo, Solapamiento and Zoom, off-screen on a normal window, and the feature read as "you can't edit anything". When a section only appears because something is selected, it is the most relevant thing on screen.
- **Typing a size must not permit what dragging a handle forbids.** A regular polygon needs `w === h` and `isValidElement` **rejects** deformed ones, so free width/height would produce a project that no longer imports; a group scales uniformly for the same reason (its pieces carry that invariant). `applyGeometry` mirrors `resizeTo`/`resizeGroupTo`: the side you just typed wins and the other follows.
- Scale before translate — the factor is relative to the *original* box.
- `syncGeometryControls` never overwrites the field that currently has focus, or typing "300" would be rewritten to "3" mid-keystroke.

Since v2.10.0 the X/Y/Ancho/Alto quartet also exists inside each of the four settings modals (`GEO_PREFIXES` = `el`, `stroke-modal`, `shape-modal`, `text-modal`, `ui-modal`): one `syncGeometryControls`/`applyGeometry(prefix)` body serves all five sets, so a measure typed in a modal obeys the exact same invariants as the panel. The modal blocks (`#*-modal-geo`) appear only with a selection, and their wrapper `<div>` is deliberately **classless** so the user-agent's `[hidden]` rule works without adding entries to the `_panel.scss` grouped rule. The wiring passes the prefix through a closure — passing `applyGeometry` directly as a handler would feed it the event as `prefix`.

Four hard-won rules inside `applyGeometry` (auditoría v2.10.1 — each has a BUGS.md entry and a guard; breaking any of them re-opens a real bug):

- **"Changed" is judged against what the field DISPLAYED** (`Math.round`), never against the exact box — otherwise every fractional box (any diagonal polygon drag; with the 120% auto-fit zoom, nearly everything) makes the untouched width "win" over the height the user just typed. When a field didn't change, the *exact* value is used, not the rounded one.
- **`Number('') === 0`**: an emptied (or garbage-typed) `type=number` field must count as "unchanged", or emptying Ancho collapses the element to 1px.
- **The no-op guard checks the *effect*** (`dx/dy/sx/sy`), not box equality: typing a height on a horizontal line (where `scaleElement` forces `sy = 1`) must not stack a phantom undo step, and the field must resync to the truth.
- **`geoFieldsFor` discards stale changes**: in a browser, clicking element B fires `mousedown` (selection changes) *before* the field's `blur`→`change`, so a width typed for A would land on B. The rAF resync always runs after the change, so the recorded selection identity catches it (e2e-guarded — the vm harness can't see this event order).

Reading and writing go through `selectionBounds()` — the combined box of ANY selection. `selectionGroupBounds()` stays group-only on purpose (it decides the single box *with handles*), but typing measures never needed the selection to be a building; multi-selections scale uniformly either way. And handles are drawn **only when the active tool is Mover** (`handlesOn` in `redrawNow`): with a kept selection and a creation tool, canvas gestures create — drawing scale handles there promised a resize that `onMouseDown` (whose handle hit-test lives in the SELECT branch) would never deliver.

### The panel is contextual (v2.9.0)

The panel's `<section>`s are shown or hidden by `syncPanelSections()`, called from `redrawNow` — the same single sync point. Sections: `#panel-sec-stroke` (always), `#panel-sec-fill` (a fillable tool **or** a fillable in the selection), `#panel-sec-text` (Texto/Emoji or a `text` selected), `#panel-sec-build` (`BUILDING_TOOLS`), `#panel-sec-garden` (`GARDEN_TOOLS`), `#panel-sec-canvas` and `#panel-sec-selection` (always). Two rows inside Trazo are contextual too: `#row-dash` and `#row-double-head`. **v2.21.0 emptied `#panel-sec-stroke` of everything that changed meaning with the tool**: the ⚙ that retargeted to five modals and the **stroke-width slider** (`#stroke-slider`/`#stroke-val`/`#stroke-label`), which with the Eraser became its size — retitled and re-ranged. What is left there is the colour palette and the two line modifiers, under a plain «Trazo» heading. The width now lives only in the five settings modals, where `#modal-text` gained its own (`#text-modal-stroke`) so a selected text isn't left without one. Each remaining section carries **its own ⚙**, except that one and `#panel-sec-canvas` (no modal holds its controls) — see "A tool's own settings…" below for the rule and for why only `#btn-element-settings` still needs a `hidden`. Before this, drawing with the pencil still showed "Plantas", "Cubierta del alzado" and "Ancho del camino" — 28 controls, of which ~10 applied. Below 1100px the panel is a drawer, so every extra control is paid in scroll.

Three things this rests on, each of which has already broken something:

- **It keys on the tool AND the selection, never the tool alone.** Fill, stroke and text controls have dual semantics, so with a rectangle selected `#panel-sec-fill` must be there even if the active tool is the pencil — otherwise selecting a shape stops letting you edit it, which is worse than the long panel. Guarded by *"las secciones reaparecen con la selección, no solo con la herramienta"*.
- **`hidden` does not hide anything that declares `display`.** `.panel__section`, `.panel__check`, `.panel__field` and `.panel__gear` all do, and that beats the user-agent's `[hidden]{display:none}` — the same trap that made `.btn[hidden]` necessary. They share one grouped rule in `_panel.scss`; **add any new `.panel__*` you plan to hide from JS to that group.** It has bitten four times, including the ⚙ from the day it was written (visible with every tool, opening the eraser modal from the pencil). The vm harness cannot see it — `hidden` there is a JS property, not CSS — so `tests/smoke.test.js` asserts all four compiled rules and `e2e/panel.spec.js` asserts real visibility. **Since v2.21.0 `.panel__gear[hidden]` is what hides `#btn-element-settings`, the only gear JS still hides** — don't delete that rule on noticing that the «Trazo» gear it was written for no longer exists.
- **Nothing was renamed or deleted.** `dom-stub.js` fabricates an empty `<div>` for an unknown id, so a renamed control fails as a confusing value diff instead of a missing element; and the Edificios/Camino twins are pinned by three guards. The reorganization only moves nodes.

`#modal-stroke` and `#modal-shape` are the drawing and shape tools' own settings, twinned with the panel through `syncStrokeControls()` / `syncShapeControls()` and opened from `selectTool` like every other configurable tool — see the rule below. `#modal-shape` also owns the **rotation** of the shapes that store it as an angle (`ROTATABLE_TOOLS` = the regular polygons, the two stars, the trapezoid and the irregular triangle; rect/roundedRect serialize a quarter-turn into their `w`/`h` and `isValidElement` **rejects** a `rotation` on them). `state.shapeRotation` is the creation default, snapped to the active type's `ShapeRotation.step` — 36° is not a valid hexagon orientation, and a trapezoid off a quarter-turn fails import validation. `creationRotation()` is the single source for both the drag preview and the committed element, and editing a selection goes through `rotateTo()`, which applies whole `ShapeRotation.rotateElement` steps rather than writing the angle directly — the trapezoid also swaps `w`/`h` on each quarter, so a raw write would leave its box out of line with its silhouette. Both sync functions write **both** sides (panel and modal): the dual-semantics handlers were extracted into named `apply*` functions (`applyDash`, `applyDoubleHead`, `applyFill`, `applyFillTransparent`, `applyFillOpacity`, `applyFillColor`, `applyStrokeWidth`) so the two control sets share one body, the way `wireBuildPair` does for the Edificios twins.

### Los ajustes de una herramienta se abren desde la herramienta

**Detalle completo: `docs/ajustes-de-herramienta.md` — léelo antes de añadir o recablear un modal de ajustes.** La regla: si una herramienta tiene ajustes, elegirla en el sidebar los abre (`selectTool` → `$('modal-X').showModal()`); un mando del panel puede reabrirlos pero **nunca ser la única vía**, y **jamás cambiar de significado con la herramienta activa** (por eso v2.21.0 eliminó el slider de trazo reconvertido en tamaño del borrador). Sin excepciones «porque es la herramienta más usada»: el usuario rechazó justo esa para el lápiz.

Mapa: `STROKE_TOOLS` → `#modal-stroke`; `SHAPE_TOOLS` → `#modal-shape` (que además posee la rotación: `ROTATABLE_TOOLS`, `state.shapeRotation` cuantizado con `ShapeRotation.step`, `creationRotation()` única fuente de preview+commit, edición vía `rotateTo()` con pasos enteros); Texto → `#modal-text`; Botón/Input/Imagen/Navbar/Tarjeta (`UI_MODAL_TOOLS`) → `#modal-ui` (Rótulo vía `applyLabel`, dual: selección o `state.uiLabels[tool]`, persistido y validado). Los catálogos que exigen elección previa están en `opensVariantModal` (cancelar restaura la herramienta anterior; un catálogo previo cae a `TOOLS.SELECT`); Borrador, Emoji, stroke, shape, text, ui y select **no** lo están: cerrarlos te deja en la herramienta, lista para dibujar.

Desde v2.21.0 cada ⚙ del panel pertenece a su sección y abre SIEMPRE el mismo modal, llamando a `open*Modal()` directamente (Edificios y Jardín son la excepción y pasan por `selectTool`, porque el catálogo se reconstruye al abrir). «Trazo» y «Lienzo» no llevan ⚙ **a propósito** (guardado en smoke). Solo `#btn-element-settings` lleva `hidden`: sigue el tipo de lo seleccionado (`TYPE_SETTINGS_MODAL`, inverso de `MODAL_EDIT_TYPE`; tipos dispares ⇒ `null`, regla de `commonOf`). `renderStrokePreview`/`renderShapePreview` leen el tipo del elemento seleccionado antes que el de la herramienta.

**Pulsar la herramienta del elemento seleccionado lo edita** (`MODAL_EDIT_TYPE`, tipo exacto — el Semicírculo mapea a `curveArrow`); empezar un gesto de creación en el lienzo suelta la selección conservada. **Mover y «Select» (`SELECTION_TOOLS`) nunca vacían la selección** — son las herramientas que trabajan sobre ella (guardado en ambos sentidos, más un tercero que fija que las de creación sí limpian). `selectTool(id, {silent: true})` selecciona sin abrir nada — obligatorio en toda activación automática (pegar, Ctrl+A, retorno de catálogo cancelado). Los defaults en español de los componentes UI («Botón», «Escribe aquí...», «Título», nav «Inicio/Nosotros/Contacto») están duplicados en renderer y exporter: cambiarlos en ambos o los exports mienten. `e2e/helpers.js` cierra los `SETTINGS_MODALS` tras elegir herramienta (un `<dialog showModal>` deja el lienzo inerte); los catálogos quedan fuera de esa lista a propósito.

### «Select» (solo selección, v2.11.0)

`TOOLS.PICK` («Select», grupo Edición, entre Mover y Borrador) es una herramienta de **solo selección**: el clic selecciona con exactamente la misma semántica que Mover (grupo completo vía `groupIndicesOf`, Alt aísla la pieza, doble clic desciende, Shift hace toggle vía el helper compartido `shiftToggleAt`, y «Los clics acumulan selección» añade/retira), y el arrastre dibuja **siempre** marquesina — incluso naciendo encima de un elemento, el gesto que con Mover lo movería y que era imposible de hacer en un lienzo denso. Nada se desplaza jamás con ella. Cuatro decisiones que hay que conservar:

- **El gesto entero se resuelve en el bloque de marquee de `onMouseUp`** (vía `state.pickDown`): hasta soltar no se sabe si fue clic o marco, así que el `mousedown` no toca la selección — un clic necesita la selección vieja intacta para poder acumular o retirar, y un marco la sustituye él mismo con `setSelection`. Con Mover ese camino no se pisa: su clic se resuelve en `mousedown` y su marquesina solo nace en el vacío.
- **No dibuja handles** — `handlesOn` en `redrawNow` sigue siendo `state.tool === TOOLS.SELECT` a propósito: un handle promete un resize que el hit-test de PICK nunca haría (regla v2.10.1: no dibujar promesas que el mousedown no cumple).
- **El doble clic desciende a la pieza pero NO abre editores** ni resetea curvaturas: es una herramienta que no modifica nada. La rama `picking` del handler dblclick corta antes de los editores de texto.
- **Sin atajo** (las 26 letras y los 10 dígitos están cogidos — misma situación que Balcón). **Desde la v2.17.0 sí tiene modal de ajustes**, `#modal-select`, que se abre al elegirla como el de cualquier herramienta configurable: contiene «Los clics acumulan selección», que dejó el panel. **Lo abren las DOS herramientas de Edición** (`SELECTION_TOOLS`, v2.18.0 — la 2.17.0 lo reservó a «Select» y dejaba a Mover dependiendo del ⚙): la casilla gobierna el clic de ambas, así que ambas la enseñan. Lo reabre el **⚙ de «Elementos»** (`#btn-selection-settings`, v2.21.0 — antes era el de «Trazo», que solo estaba con las dos de Edición), y desde ahí se alcanza con **cualquier** herramienta puesta: la casilla gobierna el clic, no el dibujo. Consecuencia obligatoria: **las cuatro activaciones automáticas de Mover pasan `silent`** — pegar una imagen, pegar elementos, Ctrl+A y el retorno de un catálogo cancelado (`wireBuildModalCancel`). Ahí nadie ha pulsado la herramienta, y un `<dialog showModal>` dejaría inerte el lienzo justo después de pegar o de seleccionarlo todo; el del catálogo, además, encadenaría un modal encima del que se acaba de cerrar. Guardado por *"Ctrl+A y pegar no abren el modal de selección"* y *"cancelar un catálogo que cae en Mover no encadena el modal de selección"* (esta última verificada fallando al quitarle el `silent`). `#modal-select` no está en `opensVariantModal` (cerrarlo deja la herramienta puesta) y sí en `SETTINGS_MODALS` de `e2e/helpers.js` — sin eso, todo spec que use «Select» se cuelga con el lienzo inerte. **Cada modal cablea su propio botón «Cerrar»** en `setupModals`: olvidarlo no rompe ningún test del arnés vm (allí se cierra llamando a `close()`) y deja la app bloqueada en el navegador, así que la guarda vm cierra por el **botón**, no por la API. El cursor es `default` (`.canvas-area__canvas--pick`, CSS puro que el arnés vm no ve — guardado en `tests/smoke.test.js`), y desde la v2.36.0 el de **Mover** es `pointer`, la mano que señala con el índice —el gesto de su propio icono ☝️—, no las cuatro flechas de `move`: el `move` prometía desplazamiento incluso sobre el lienzo vacío, donde esa herramienta empieza una marquesina. Se usa el cursor nativo y no una imagen del emoji porque es nítido a cualquier densidad de pantalla y su punto activo cae donde debe; guardado en `tests/smoke.test.js` por el mismo motivo que el de «Select». El orden exacto del grupo Edición (`select`, `pick`, `eraser`) y su condición de única herramienta sin tecla del grupo están fijados en `tests/config-templates.test.js`; el comportamiento, en `tests/app-interaction.test.js` y `e2e/select-tool.spec.js`.

### Aerógrafo (v2.22.0)

**Detalle completo: `docs/aerografo.md` — léelo antes de tocar airbrush.js o sus mandos.** Lo innegociable: `airbrush` **SÍ es un tipo de elemento** — jamás en `CREATION_ONLY_TOOLS` o desaparece al reimportar (guardado por round-trip). El elemento guarda solo el eje (`points`) + `radius`/`density`/`lineWidth`/`seed`; las gotas se regeneran donde se necesitan con el hash sin estado `_rnd` (nunca `mulberry32`: una secuencia re-tira la nube entera — *boiling*), y `t` sale del hash, no de `i/n`. Reparto radial 2-D isotrópico (`radius` es cota dura: bounds/clip/borrador exactos); `MAX_DOTS` degrada la tasa, nunca corta; `getElementBounds` devuelve la BANDA (`Airbrush.visibleBox`); resize uniforme — `radius` escala, el grano (`lineWidth`) no. Sólido = una ruta con N `arc()` (cada uno con su `moveTo`) y un único `fill()`; translúcido = `globalAlpha` + `fill()` por gota; en SVG `<g fill fill-opacity>` — **`fill-opacity`, jamás `opacity`**. La ausencia del campo es el aspecto por defecto: `opacity` solo si < 1, `clip` solo si hay área (el elemento guarda una **copia** del área, nunca la referencia; el marco es overlay puro). Armar el área **cierra el modal** (dialog inerte — fallo v2.16.2); un arrastre bajo `MIN_AREA` no cambia nada y sigue armado; marcarla no lleva `saveUndo`. Una mancha con todas las gotas fuera del área no se crea. El seed se fija en `mousedown` (la preview usa `Renderer.renderElement`, no `drawPiecesPreview`). El puntero es el círculo de la boquilla (`cursor: none`; se recorta el TAMAÑO de las gotas, no el centro). La paleta existe dos veces (`COLOR_GRIDS`): acota la rejilla en tests (`#color-grid .panel__color-swatch[...]`). Sus cuatro sliders van por `applyAirbrush`, nunca `applyStrokeWidth`. Calibrado en navegador: grano 5 / densidad 70 / `MAX_DOTS` 6000.

### Sólidos 3D (v2.24.0)

**Detalle completo: `docs/solidos-3d.md` — léelo antes de tocar solid.js, sus cuatro modales o `regenerateSolid`.** Lo innegociable: toda herramienta 3D es de solo creación — `CREATION_ONLY_TOOLS` debe incluir `...SOLID_TOOLS`. En proyección caballera la tapa trasera es la frontal trasladada, así que **la cara frontal se emite como el elemento 2D real de su sección** (por eso renderer/exporter/borrador/bounds no llevan ni una línea 3D); el resto son `line`/`curveArrow` — los únicos tipos que aceptan `dash` — y caras laterales `polygon` con `stroke: false` (solo las vistas). Visibilidad de cara = **signo del área del cuadrilátero proyectado** (shoelace contra σ del perfil), no `n·d`; empate ⇒ visible; `_occlude` es una segunda pasada obligatoria (cóncavos y `k < 1`); la esfera queda fuera de `_occlude` (su ecuador es superficie, no arista). **Jamás `arc: true` en una pieza 3D** — app.js lo renormaliza a semicírculo exacto al primer toque; los arcos elípticos van como `curveArrow` encadenado en tramos ≤90° con igualdad estricta en las juntas. Orden de emisión: `[ocultas, caras, FRENTE, vistas]` — el relleno delante se comería el ecuador. **Un sólido relleno lleva SIEMPRE `fillColor` explícito** (sin él, `Renderer.fillStyle` cae al tinte 12% del trazo — fallo v2.25.4). `solidMeta` guarda solo lo no deducible (remate/proyección; de pie añade `apex`/`section`/`rotation`/`gesture` + opcionales `turns`/`fillColor`) y **viaja con la figura al mover, escalar y girar**; el ángulo de fuga sí gira con ella. `regenerateSolid` reconstruye desde la cara frontal (el relleno lo dice una CARA, nunca una arista; los sliders regeneran en `change`, nunca `input`); `selectTool` conserva la selección solo si `selectedSolid()` ve la figura completa; `syncSolidControls` lee con `commonOf` (varias piezas siempre). De pie (`solidApex: 'upright'`, Pirámide/Tronco): la ausencia del campo reproduce byte a byte el modo clásico; un solo cuerpo con factor de tapa `k`; solo se ve tapa o base según `d.y`, y con relleno esa cara se pinta la última. `drawPiecesPreview` pone `setLineDash` por pieza y delega a `Renderer.renderElement` lo que no sea line/rect/circle/curve, con seed fijo. Los tres modales de extrusión comparten `state.solidSection` (`VARIANT_MODALS`); la Esfera es solo ajustes y necesita su propio «Cerrar» en `setupModals`.

### Los modales de ajustes: ancho, bloques y paletas (v2.26.0)

**Detalle completo: `docs/modales-de-ajustes.md`.** Los diálogos con miniatura llevan `modal--settings` en el `<dialog>` — olvidarla no rompe nada visible, solo deja el modal estrecho (guardas: recuento en smoke + medida real en `e2e/modal-palette.spec.js`) — y organizan sus mandos en bloques `.modal__group` (`<fieldset>`/`<legend>` reales, con el reset de `min-width: min-content` y la regla `.modal__group[hidden]` obligatorios; si el único mando de un bloque se oculta, se oculta el bloque entero; un bloque no repite su título en un campo de dentro). Los títulos exactos por modal están pinneados en `e2e/modal-palette.spec.js`. Dentro de un modal la paleta fija columnas a `2.8rem` y el selector de color vive dentro de su `.modal__palette`. **Dos familias de paletas, y la separación es el punto**: `COLOR_GRIDS` (`.panel__color-swatch`, color del trazo, `updateColorActive`) y `FILL_COLOR_GRIDS` (`.panel__fill-swatch`, relleno, `updateFillColorActive`) — comparten aspecto y no clase porque el resaltado va por clase. Un clic en muestra de relleno pasa por `applyFillColor` y cierra su gesto en el acto (`fillSwatchApply`). Todo test que busque una muestra por `data-color` debe **acotar la rejilla** o falla en modo estricto.

### Emoji

`TOOLS.EMOJI` is creation-mode only, like `TOOLS.ARC` — **not an element type**. Picking the tool opens the `#modal-emoji` catalogue (built from `EMOJI_GROUPS` in config.js) — respecting `selectTool`'s `silent` flag, or cancelling a Building/Garden catalogue with Emoji as the previous tool would chain this one on top (auditoría v2.10.1) — and `placeEmoji()` stamps the chosen character as an ordinary `text` element centred on the click, at `state.emojiSize` — its own setting since v2.10.0 (the `#emoji-modal-size` slider in the catalogue, `EMOJI_MIN_SIZE`–`EMOJI_MAX_SIZE` = 32–96, persisted in prefs), no longer tied to `state.fontSize`: growing an emoji must not shrink the next text. With Emoji active and no selection, the panel's «Texto» section **retargets** to it (`#font-label` says «Emoji», the slider drives `state.emojiSize` through `applyEmojiSize`, min 32) — this is the last surviving instance of that pattern, and it is worth knowing that its twin, the stroke slider retargeted to the eraser's size, was **removed** in v2.21.0 for exactly the reason a retargeting control is suspect; before that retarget the section was a dead control for the Emoji, and pre-2.10.0 the slider *did* govern it. Both font sliders reach `EMOJI_MAX_SIZE` (96) so a selected big emoji doesn't overflow them. Because it is just a `text` element, rendering, hit-testing, resize, undo, autosave and all five export formats work with no emoji-specific code — keep it that way rather than introducing an `emoji` type. The catalogue is injected with `textContent`, never `innerHTML`.

### Edificios (sección)

**Detalle completo: `docs/edificios.md` — léelo antes de tocar building.js, sus diez catálogos, `#modal-facade` o el comportamiento de grupos.** Lo innegociable: toda herramienta de Edificios es de solo creación — `CREATION_ONLY_TOOLS` debe incluir `...BUILDING_TOOLS`. Cada una abre su catálogo al elegirla; Balcón, Muro, Verjas, Cancela e Iluminación (v3.15.0, 8 modelos de alumbrado) van por la tabla genérica `VARIANT_MODALS` (iconos = geometría real), las otras cinco por pares `build*Catalog`/`update*Active` a mano con SVG inline (siempre `createElement`/`textContent`, nunca `innerHTML`; cada `update*Active` acotado a su catálogo). **`buildOpts()` es la única fuente de opts** para toda llamada a `Building.elements` (preview, commit y miniatura) y **`syncBuildControls()` el único punto de sincronía** de los gemelos panel↔`#modal-facade` (opciones y rangos idénticos, guardado en smoke). En un modal híbrido catálogo+ajustes, un ajuste alcanzable solo en la otra variante es un callejón sin salida (regla v2.3.1 — clicar una vista cierra el modal). Los ajustes del panel de Edificios son solo defaults de creación (sin semántica dual) y persisten en prefs. `FACADE_TYPES` no promete cubierta en el nombre (guardado). Muro/Verjas/Cancela: híbridos con vista + modelo + altura física en cm y preview vivo; comparten `GATE_TYPES` (18) y `FORGE_TYPES` (13); sus límites exportados deben coincidir con los sliders del HTML (smoke). Iluminación es el cuarto híbrido desde la v3.16.0 (y el único `modal--settings` sin miniatura): en `mast` y `tower` la altura sale de la cota en metros de `LIGHT_SPORTS`/`state.lightMastM` y NO del arrastre, se anclan por la base, su caja no está en `byVariant` (el ancho acompaña a la cota) y el icono va con cota fija `LIGHT_ICON_M`.

Grupos: cada edificio estampa un `buildingGroupId` compartido; `groupIndicesOf` expande el clic al grupo; doble clic desciende a la pieza y Alt+clic es el acelerador (la regla de una mano cubre también modificador+clic); «Los clics acumulan selección» (`state.multiSelect`, no persistido) añade/retira vía `pendingUnselect`. **`hitTest` corre antes de la rama del marco combinado, y esa rama cede si el punto cae en un elemento de fuera de la selección** (o lo dibujado encima de un edificio queda inalcanzable — guardado, y el tercer test pinnea la conveniencia misma). El hit-test de handles corre **antes** del arrastre del marco. El resize de grupo (y de cualquier multiselección desde v2.12.0) es **uniforme** — un estiramiento libre rompería `w === h` de los polígonos y el proyecto no reimportaría — y `scaleElement` mapea las coordenadas propias de cada pieza, nunca asigna la caja destino. `insertClones` reasigna un `buildingGroupId` nuevo por edificio clonado; `isValidElement` valida el id (`ID_RE`). **Los mandos del panel enseñan el valor común de la selección (`commonOf`) o no tocan nada** — y la regla ata también a `syncStrokeControls`/`syncShapeControls`/`syncTextControls`, que escriben en ids del panel (fallo v2.16.3: los defaults machacaban el valor común en cada repintado). Balcón es la única herramienta con caja por variante (`variantKey`/`byVariant`; su icono usa drag nulo a propósito); Balcón, Muro, Verjas y Cancela no tienen atajo (lista exacta pinneada en `tests/config-templates.test.js`).

### Jardín (sección)

**Detalle completo: `docs/jardin.md` — léelo antes de tocar garden.js, `VARIANT_MODALS` o Caminos.** Lo innegociable: ocho herramientas de solo creación (`CREATION_ONLY_TOOLS` debe incluir `...GARDEN_TOOLS`), 72 variantes; agrupan con `buildingGroupId` y las plantas repiten un `gardenMeta` estricto y versionado en cada pieza (`moveElement` desplaza sus puntos de inserción; cloning lo conserva bajo id nuevo; el exporter valida campo a campo). Catálogos por la tabla genérica `VARIANT_MODALS` → `buildVariantCatalog`/`updateVariantActive` (`opensVariantModal` es el predicado común); los iconos son la geometría real (`variantIcon`; las plantas usan `plantIconBox()` — la proporción botánica, no el drag, o dos especies comparten icono borroso). **`drawPiecesPreview` debe cubrir todo tipo que una herramienta pueda emitir** — lo que falte no se dibuja, sin error (las cadenas de `curveArrow` van por `CurvePath.segments`; `text` se delega a `Renderer.renderElement`). Atajos: `8 9 h x z` eran las únicas teclas planas libres; toda herramienta nueva llega sin tecla, y `f q d s` son acciones de curva comprobadas antes de `TOOL_KEYS` (`RESERVED_PLAIN_KEYS` pinnea lista y huérfanos). Una variante emite **una sola** etiqueta `text` (la de `_labelled`) y con `labels:false` ninguna — cualquier rótulo interno se resuelve con geometría (la «N» del norte, la escala sin cifras). Dos variantes de un catálogo nunca se dibujan igual (guarda: multiset de tipos + proporción). En las tablas `LOBES`, alternancia sobre pocos puntos = polígono. Caminos: herramienta propia con 4 variantes (trazado × pavimento, tabla `PATH_VARIANTS`, jamás sniffing del id); el arrastre es una caja — lado largo = ruta, corto = grosor —; el ángulo libre es el ajuste sticky `state.pathAnyAngle` (checkbox duplicado panel+catálogo) con Shift como acelerador transitorio (`state.pathFreeAngle`, reseteado explícitamente al final del commit porque los iconos leen `gardenOpts()`): **un modificador jamás es la única vía — regla de una mano, restricción permanente**. El icono no enseña el ángulo (caja fija); la miniatura `renderPathPreview()` sí. Una sola geometría en coordenadas de camino (`u`/`v`, `_p(u,v)` rota; la onda se calcula una vez en `_at`); `drawPathAngle()` es overlay puro. El panel conserva el interruptor global de etiquetas y los mandos del Camino; los defaults botánicos viven en cada modal de planta y persisten. Selección completa de grupo de planta ⇒ `#btn-edit-garden` → `editSelectedGarden()`/`regenerateGardenGroup()` con un solo undo; cancelar limpia `editGardenGroupId` sin tocar el original. **La parra malvasía (v3.4.0–3.4.5) es la única especie con geometría propia** —una pérgola, despachada desde `_treeTool`/`_treeElevation`— y vive entre los ÁRBOLES aunque la vid sea trepadora: lo que se dibuja es una estructura de sombra exenta. Dos reglas suyas que valen para todo el jardín: **el relleno jamás va en un `_blob`** (es un `_chain`, o sea un `curveArrow`, y el renderer lo traza sin mirar `fill` — fallo mudo de punta a punta), sino en un `polygon` con `stroke: false` y el contorno encima por los mismos puntos; y **una superficie es UNA pieza**, porque varias translúcidas del mismo color acumulan alfa en los solapes y dibujan sus costuras. Ambas guardadas en `tests/garden.test.js` y con entrada propia en `BUGS.md`.

**La salida de un modal de ajustes debe sobrevivir a que el modal crezca (v2.16.2)**: `.modal__cancel` es `position: sticky; bottom: 0` — nunca negativo, sin margen negativo, y con la segunda sombra sin desenfoque que tapa el padding del diálogo (un botón de cierre bajo el viewport deja la app entera inerte). Guardado en `e2e/modal-fit.spec.js` a 1160×560.

### Text style: bold and shadows (v2.16.0)

`text` elements take three optional fields — `bold` (boolean), `shadow` (a `TEXT_SHADOWS` id) and `shadowColor` (hex) — and **their absence is the classic look**, so older projects render byte-identically. `Renderer.textFont(el)`/`Renderer.applyTextShadow(ctx, el)` are the single resolution point, mirrored by `_svgTextShadow`/`_htmlTextShadow` in exporter.js so the five formats can't drift. Seven things to keep:

- **Removing a style deletes the field, never stores it false.** `applyTextStyle` drops `bold:false` and `shadow:'none'` (plus its orphan `shadowColor`), which is what keeps an unstyled text serializing exactly as it always did instead of bloating every element in the JSON. Guarded by *"quitar el estilo lo BORRA del elemento, no lo guarda en falso"*.
- **Shadow measurements scale with `fontSize`** against `SHADOW_REF_SIZE` (18). A fixed 2px shadow is invisible on a 60px title. The SVG's `stdDeviation` is **half** the canvas blur radius — same number would come out twice as diffuse.
- **The hard shadow's offset is deliberately tiny (1px at the reference size).** With no blur to soften it, a larger gap stops reading as a shadow and starts reading as the word typed twice — which is exactly what 3px did on a 44px title. Verified in a browser comparing 3, 2, 1.5 and 1.
- **`isValidElement` validates all three**, because they end up in exported markup: an invented shadow id would put an arbitrary filter id in the SVG, and a non-hex colour is free text inside a style attribute. The HTML exporter also strips `;<>{}"'` from the colour rather than trusting that everything reached it through import validation.
- **Bold is only real where the family has a 700 cut** (Caveat, Kalam, Montserrat Alternates, OpenDyslexic); the other three are single-weight and the browser synthesizes. Never fake a 700 `@font-face` pointing at the 400 file — the browser accepts it and bold comes out *identical* to regular, which is worse than synthesis. That was Caveat's bug until v2.16.0 and `tests/smoke.test.js` now pins that no declared bold shares its file with the regular.
- **Every continuous control is ONE undo step per gesture, and a colour picker is a continuous control.** The native `<input type="color">` dialog fires an `input` per *tone crossed while dragging*, so a `saveUndo()` per event fills the 50-deep history with intermediate tones and evicts the user's actual work — silently. `applyTextShadowColor` shipped without the pattern in v2.16.0 and had to be fixed in v2.16.3; the fix is the same `snapshot()`-on-first-input + one `pushUndo` on `change` that `applyColor`, `applyFillColor`, `applyStrokeWidth`, `applyFontSize`, `applyFillOpacity` and `applyShapeRotation` already use (and the default is persisted on the commit, not per tone, or the drag hammers `localStorage` too). Guarded for **all three colour pickers at once** by *"ningún picker de color apila más de un paso de undo por gesto"*, plus a 60-tone drag (over the 50 limit) in *"un arrastre por el color de la sombra es UN paso de deshacer, no uno por tono"*.
- **Changing the shadow TYPE must not stamp the colour.** `applyTextShadowType` used to add `shadowColor: state.textShadowColor` to the patch, and the spread beat the element's own — but with a selection the picker writes to the *element*, not to the default, so `state` stayed grey and switching «suave»→«halo» reverted the red just chosen. A text with no `shadowColor` renders with `DEFAULT_SHADOW_COLOR`, which is exactly what the picker shows for it, so stamping nothing is what keeps control and drawing in agreement (and serializes leaner). Creation *does* stamp it, in `textStyleDefaults()`, where the default is what the picker shows. Guarded by *"cambiar el TIPO de sombra conserva el color propio del texto"*.

### Orden Z de la selección (v2.39.0)

Cuatro acciones para recolocar la selección en el apilado — al frente, adelantar, retrasar, al fondo —, la idea de Excalidraw. **El orden del array `state.elements` ES el apilado**, así que `reorderSelection(dir)` (app.js) es lo único nuevo: renderer, exportadores, «Bordes ocultos», la Tinta y el borrador no necesitan ni una línea. Cinco reglas:

- **La selección conserva SIEMPRE su orden relativo** (extraer y reinsertar, nunca ordenar): un edificio es un bloque contiguo y reordenarlo debe dejarlo contiguo y con sus piezas en el mismo orden interno — guardado pieza a pieza con una fachada.
- **'up'/'down' saltan UN vecino no seleccionado entero**, con barridos de intercambio (descendente para subir, ascendente para bajar), así que el bloque cruza al vecino de una vez en lugar de entremezclarse con él.
- **En el tope no pasa nada**: si el resultado es idéntico elemento a elemento, ni undo ni redraw — la regla del no-op de `applyGeometry`. Tras reordenar, `state.selection` se recompone buscando los MISMOS objetos por referencia (elementos inmutables).
- **Los botones son la rejilla `.panel__zorder`** (2×2, en «Elementos», ocultos sin selección) — la clase declara `display: grid`, así que está en la regla agrupada de `[hidden]` de `_panel.scss` (sexta vez que aplica la trampa).
- **Los atajos son `Ctrl/Cmd+↑/↓`** (un paso; con `Shift`, a los extremos), **flechas y no corchetes a propósito**: en el teclado español `[`/`]` exigen AltGr y la regla de una mano manda. El bloque corre antes del giro de `←`/`→` y del NUDGE en el keydown, y filtra `e.repeat` como ellos. Guardado en `tests/app-interaction.test.js` (pasos, extremos, undo sin fantasma, edificio como bloque).

### Guías de alineación al arrastrar (v2.38.0)

Al arrastrar una selección con **Mover**, sus bordes y centros se **imantan** (tolerancia `ALIGN_TOL` = 5 px en espacio lienzo) a los bordes y centros de los elementos no seleccionados, y una guía discontinua cruza el lienzo enseñando con quién quedó alineada — la idea de Excalidraw/tldraw. Todo vive en `alignAdjust()` (app.js), llamado desde la rama de arrastre de `onMouseMove` **antes** de `moveSelectionBy`, así que `clampDelta` sigue teniendo la última palabra en el borde. Cinco cosas que hay que conservar:

- **La guía es overlay puro** (precedente exacto: `drawPathAngle` y el marco del aerógrafo): `state.alignGuideLines` se pinta al principio de `paintOverlay` y se limpia con él cada fotograma — jamás elemento, ni undo, ni autosave, ni export. El `scheduleOverlay()` del mouseup es lo que evita que la última guía quede colgada tras soltar.
- **`free` acumula el delta CRUDO del puntero** (la sesión `state.alignSession`, nacida en el primer fotograma del gesto y muerta al soltar, como `eraserSession`): el imán corrige respecto a esa posición libre, así que al salir de la tolerancia el objeto vuelve con el puntero **sin zona muerta** — la misma razón por la que `dragLast` guarda la posición real y no la recortada.
- **Los candidatos se calculan una vez por gesto**, no por fotograma: los bordes/centros de lo no seleccionado no cambian mientras se arrastra. La excepción que lo haría mentira se excluye: una flecha **anclada** a lo seleccionado viaja con ello (`resolveAnchors`), así que no es candidata — sin eso el imán pegaba a su posición del primer fotograma, una coordenada ya vacía (auditoría v2.39.1).
- **El mando es «Guías de alineación» en `#modal-select`** (`state.alignGuides`, en prefs, **activa de fábrica**), y **Alt la suspende en caliente como acelerador, nunca única vía** (regla de una mano, mismo convenio que el snap a cuadrícula). Al contrario que «Los clics acumulan», sí persiste: es un modo de trabajo. **`free` acumula también en los fotogramas con Alt** — la salida temprana pre-acumulación hacía que al soltar Alt la selección saltara hacia atrás justo lo recorrido suspendido (auditoría v2.39.1).
- **Si el imán acaba de pegar, la cuadrícula NO re-snapea al soltar — pero solo EN SU EJE** (`alignSession.snappedX`/`snappedY` en el bloque de mouseup): gana la guía, que es más específica, y el eje donde no pegó nada sigue siendo de la rejilla — con un flag único, imantar la X dejaba la Y sin imán y sin cuadrícula a la vez (auditoría v2.39.1). Guardado en `tests/app-interaction.test.js` (imantado exacto con la casilla puesta, posición libre con ella quitada, y los tres casos de la auditoría).

### Lápiz con presión simulada (v2.37.0)

El lápiz puede dibujar con **grosor variable según la velocidad del gesto** — rápido = fino, lento = grueso, extremos afilados —, la idea de [perfect-freehand](https://github.com/steveruizok/perfect-freehand) (MIT) reimplementada en `src/js/freehand.js` (global `Freehand`, módulo puro: sin DOM, sin `Math.random`). El campo es **`taper: true` en el `pencil`, y la ausencia es el lápiz clásico** — un proyecto viejo serializa y se dibuja byte a byte igual. Seis cosas que lo sostienen:

- **El contorno se regenera donde se necesita** (renderer, export SVG, previsualización del gesto, muestra del modal) a partir de los mismos `points` de siempre — el JSON no guarda el polígono, igual que el aerógrafo no guarda sus gotas, así que los cuatro dibujos no pueden divergir. `Freehand.outline(points, lineWidth)` es determinista: mismo gesto, mismo polígono.
- **La envolvente respeta el grosor nominal**: el semiancho máximo es `lineWidth/2`, exactamente lo que ya ocupaba el lápiz clásico, y por eso bounds, hit-test y el alcance del borrador (`r + lineWidth/2`) valen sin tocarlos. No subas el tope «para que luzca»: romperías esa cota en tres sitios a la vez.
- **Se dibuja RELLENANDO el polígono, no trazándolo** — en el canvas (`fill()`) y en el SVG (`<path … Z" fill stroke="none"/>`). Un stroke del contorno daría el doble de grosor aparente y divergiría del lienzo. No pasa por Sketchy: el temblor ya viene en los puntos del gesto.
- **El borrador lo trata como a cualquier lápiz** (parte por `points`), y `_asPencil` (eraser.js) **copia `taper`** al fragmento: solo un `pencil` de origen puede llevarlo (`isValidElement` lo ata al tipo y solo en `true`, patrón `ink`), así que un trozo de curva convertido a lápiz nunca lo hereda por accidente.
- **El mando es «Trazo con presión» en `#modal-stroke`**, con la semántica dual de siempre (`applyTaper`: con lápices seleccionados los edita, sin selección fija `state.strokeTaper`, que persiste en prefs). Se **atenúa, no se oculta**, cuando ni la herramienta ni la selección son un lápiz — mismo criterio que el discontinuo. Quitarlo **borra el campo**, nunca guarda `false`.
- **La calibración es de navegador, no derivada** (mismo espíritu que el grano 5/densidad 70 del aerógrafo): `SPEED_SPAN = 4` está puesto para que, con la decimación de 2 px del lápiz, un trazo pausado vaya casi a ancho completo y un latigazo caiga a `MIN_W`. Guardado en `tests/freehand.test.js` (determinismo, cota de la envolvente, contraste lento/rápido, afilado de puntas, validación y SVG) y `tests/app-interaction.test.js` (default + semántica dual).

### Tinta: el bote de pintura (v2.32.0)

**Detalle completo: `docs/tinta.md` — léelo antes de tocar flood.js o `inkBarrierMask`.** Lo innegociable: `ink` va en `CREATION_ONLY_TOOLS` — no es tipo de elemento; la mancha es un `polygon` con `ink: true` (lo que hace que una mancha previa no sea barrera y la distingue de una cara de sólido) y **`fillColor` siempre explícito** (o cae al tinte 12% — fallo v2.25.4). `Flood` (flood.js) es geometría pura — solo `Uint8Array`, números y `{x,y}` cruzan su frontera; marching squares + Douglas-Peucker sobre anillo cerrado; la región se **dilata antes de extraer el contorno** (nunca desplazar vértices: autointersección), pasarse es gratis y quedarse corto se ve; el borde del lienzo NO es barrera y el escape corta al tocarlo; un clic sobre trazo prueba varios píxeles libres alrededor. En app.js: `inkBarrierMask()` rasteriza con `renderElements` en modo `normal` (nunca `renderScene` ni `hidden-dashed`), excluye lo que lleva `ink` y rasteriza continuo lo discontinuo; caché por comparación de referencias elemento a elemento. La mancha se inserta con `splice` **por debajo** del elemento más bajo tocado, y repintar la misma zona **sustituye** comparando cajas de los ELEMENTOS (nunca la de la región). El color ES `state.fillColor`/`fillTransparent`/`fillOpacity` reutilizados (rejilla en `FILL_COLOR_GRIDS`, no en `COLOR_GRIDS`); ajustes propios `inkGap`/`inkTarget` en prefs. El cuentagotas **cierra el modal al armarse** (dialog inerte) y Alt+clic es atajo opcional, nunca única vía; «pintar la selección» y «sustituir un color» son un solo undo; `hex6` normaliza a minúsculas (v2.35.0). Elegir la Tinta conserva la selección si contiene algo rellenable. Limitaciones asumidas: las islas se pintan enteras (el modal avisa vía `Flood.countHoles`), la mancha no sigue a los trazos, y el aerógrafo es poroso por construcción.

### Shape fill

Only `FILLABLE_TYPES` (rect, roundedRect, circle, trapezoid, freeTriangle, square, triangle, pentagon, hexagon and the two stars — it spreads `REGULAR_POLYGON_TYPES`, so a new regular polygon joins by existing) take a fill; UI components carry their own as part of their design. Four fields control it: `fill` (boolean, on/off), the optional `fillColor` (hex), the optional `fillTransparent` (boolean), and the optional `fillOpacity` (number from 0 to 1, defaulting to 0.4 when absent). They are deliberately independent — the "Rellenar formas" checkbox toggles `fill` only and never touches the others, so emptying and refilling a shape recovers the same color, mode and opacity. "Relleno translúcido" and the opacity slider have the standard dual panel semantics: with a selection they update selected fillable shapes, using one undo step per slider gesture; without a selection they set creation defaults. Solid mode is represented by the absence of `fillTransparent`, preserving backward compatibility.

`Renderer.fillStyle(el)` is the single place that resolves these fields. Transparent mode converts `fillOpacity` to an 8-bit hex alpha and applies it to `fillColor || color`; otherwise it uses `fillColor || color + '20'`. A translucent shape without `fillOpacity` must remain at the legacy 40% (`0x66`). Therefore **a shape without `fillColor` keeps the classic translucent stroke tint**, and older projects render identically — preserve both fallbacks. SVG and HTML exports mirror this resolution. `isValidElement` validates `fillTransparent` as a boolean, `fillOpacity` as a finite number in `[0, 1]`, and `fillColor` as hex because they affect serialized/exported output.

### Shape overlap mode

`state.overlapMode` is a global scene setting with two valid values: `normal` (default and backward-compatible) and `hidden-dashed`. `Renderer.renderElements(ctx, elements, mode)` must be used for whole-scene rendering so it can compare geometric shapes by z-order. In hidden mode it samples the real outlines of rect, roundedRect, circle/ellipse, square, trapezoid, triangle, pentagon, hexagon and star shapes (`OVERLAP_SHAPE_TYPES`), assigns every covered segment to the highest later shape containing it, draws visible segments solid, and defers hidden segments until after that covering shape's fill. Only hidden segments use `[4 * lineWidth, 4 * lineWidth]`; the upper outline stays solid.

The setting is persisted in prefs, autosave and exported JSON (`settings.overlapMode`). `Exporter.importJSON()` keeps its array return API and exposes the imported mode as the non-enumerable `elements.overlapMode` property. PNG/JPG call the coordinated renderer; SVG and hidden-mode HTML use the same overlap plan. Old autosaves (bare element arrays) and JSON without settings resolve to `normal`.

### `←`/`→` rotate instead of nudging (v2.23.0)

With a selection where **every** element stores its orientation as an angle (`ROTATABLE_TOOLS`: the regular polygons, the two stars, the trapezoid and the irregular triangle), `ArrowLeft`/`ArrowRight` step through valid orientations instead of nudging 1 px; `↑`/`↓` still nudge, and any other element in the selection returns all four arrows to nudging. It sits **before** the `NUDGE` block in the keydown handler. Four decisions, each deliberate:

- **It costs those shapes their horizontal nudge**, and that was the user's explicit call when offered the alternatives (`Shift+←/→`, or only-when-a-single-shape). The mouse, `↑`/`↓` and the panel's X/Y fields all still move them.
- **`every`, not `some`**: with a rectangle or a text in the selection, what the arrows do would otherwise depend on what the group happens to contain.
- **`rect`/`roundedRect` are excluded** even though `ShapeRotation` handles them: there "rotating" is swapping `w`/`h`, which isn't worth losing their nudge for. So the set is `ROTATABLE_TOOLS`, not `ShapeRotation.isType`.
- **Auto-repeat is filtered** (`e.repeat`), like `NUDGE` and the F/Q/D/S toggles: holding the key would spin the shape and stack ~30 undo steps a second, evicting the 50-deep history. `preventDefault` runs *before* that check — `←`/`→` are also the canvas area's horizontal scroll, and a held key must not slide the canvas either. `e2e/rotate-keys.spec.js` guards exactly that (in `NARROW`, where the canvas overflows and there is scroll to lose; in `WIDE` it fits and the check would be vacuous).

### Coordinate handling

Zoom is applied as a CSS `transform: scale()` on the canvas wrapper; `getPos()` in app.js divides mouse coordinates by `state.zoom` so element coordinates are always in unscaled canvas space. A `transform` does **not** grow the layout box, so `.canvas-area__sizer` wraps the canvas and `applyZoom` sets its width/height to `CANVAS_W/H * zoom`; the wrapper uses `transform-origin: top left` plus `width: fit-content` (without it the wrapper stretches to the already-scaled sizer and the transform scales it a second time). Keep all three in sync or the zoomed-in canvas becomes partly unreachable by scroll (see `BUGS.md`). `fitZoomToViewport()` auto-picks the largest zoom (floored to a 10% step, never below 100%) that fits `.canvas-area`'s available size, run on init and on window resize; it backs off permanently once the user touches the zoom slider (`zoomManual` flag), so manual choices are never overridden. `ZOOM_MIN`/`ZOOM_MAX` (0.3–3) bound both `applyZoom` and the auto-fit, and **must be kept in sync with the `#zoom-slider` min/max in index.html**.

**Cámara: zoom al cursor, pan y encuadres (v3.5.0).** `Ctrl/Cmd+rueda` sobre `.canvas-area` hace zoom manteniendo el punto bajo el cursor (`zoomAtClient`); la rueda a secas sigue siendo scroll. Espacio mantenido (`spacePan`) o botón central inician pan (`panDrag`) — se resuelve en el `pointerdown` **antes** de `onMouseDown`, porque es cámara y no gesto: jamás toca elementos, undo ni autosave, y el espacio se suelta también en `blur` (un Cmd+Tab con la tecla pulsada se lleva el keyup). Encuadres: `Mayús+1` (todo), `Mayús+2` (selección), `Ctrl+0` (100 %) — **por `e.code`**, porque en el teclado español `Mayús+1` escribe «!»; sus vías de ratón son `#btn-zoom-fit` y el `#zoom-val` clicable (ahora un `<button>` dentro del `<label for="zoom-slider">` que smoke guarda). `zoomToBounds` **sí puede bajar del 100 %** (lo pide el usuario), a diferencia del auto-fit. Tres trampas: todo camino que cambie el zoom con la rueda o un encuadre pasa por `sizerInstant()` — la clase `canvas-area--instant` apaga las transiciones del sizer Y del wrapper, porque el ajuste de scroll se calcula con `getBoundingClientRect` y a mitad de animación mide una caja intermedia (el zoom por slider conserva su animación, que `e2e/helpers.js setZoom` espera); todos marcan `zoomManual = true`; y `#btn-back-content` («Volver al dibujo», `updateBackContent` desde `redrawNow` + el scroll del área) es `position: fixed` con su propia regla `[hidden]` y su `text-transform` en `_canvas-area.scss` (no cuelga de los cuatro contenedores de `_uppercase.scss`). Guardas: `tests/app-interaction.test.js` y `e2e/navigation.spec.js`.

**Feedback en vivo (v3.6.0).** Cuatro piezas, todas efímeras: el **hover** (con `SELECTION_TOOLS`, `hoverIdx` en app.js) resalta en el overlay el grupo que el clic cogería — solo repinta cuando cambia el candidato, se apaga en cualquier gesto y en `pointerleave`, y nunca sobre un grupo ya seleccionado completo; las **cotas** (`drawBadge`, la pastilla de `drawPathAngle` generalizada) enseñan `ancho × alto` al crear/redimensionar y `X, Y` al arrastrar — el Camino queda fuera de la de creación porque su pastilla es el ángulo, y **los specs de sólidos excluyen los dos colores exactos de la pastilla** (`#4ecdc4`/`#12121c`) al medir la tinta del overlay (`overlayInk`, e2e/solids.spec.js) o la comparación preview↔resultado se infla; los **toasts** (`showToast`, región `#toast-region` con `role=status aria-live` — cola de 2, autodescarte 2.2s) avisan de copiar/pegar/duplicar/exportar/abrir y sustituyen los dos `alert()` de imagen; y `redrawNow` sincroniza **`disabled` de undo/redo** con sus pilas y la visibilidad de **`#canvas-empty`** (la bienvenida del lienzo vacío, `pointer-events: none`, que `onMouseDown` aparta ya en el primer gesto). En e2e, `Ctrl+C` sintético NO despacha `copy` sin selección de texto: se dispara con `document.execCommand('copy')` (ver e2e/feedback.spec.js).

**Táctil (v3.7.0).** El cableado de punteros (app.js, «Canvas event binding») lleva un mapa `touchPts` por dedo: **el segundo dedo convierte la mano en cámara** (pan del centroide + pinch vía `zoomAtClient`) — un trazo a medias se **aborta sin cometerse** (`abortStroke`; una cadena de curvas solo suelta el tramo en curso), un arrastre/resize/marquee se remata como `pointercancel`, y la cámara dura hasta soltar TODOS los dedos (`touchCamOn`: el superviviente de un pinch no dibuja). **Doble-tap** = dblclick sintético (`maybeDoubleTap`; solo cuenta un tap sin arrastre, y el handler de dblclick filtra el duplicado nativo por ORIGEN — `e.isTrusted` pegado a un sintético — y no por reloj: un dedupe por tiempo a secas acoplaría al cronómetro tanto los dblclick del arnés vm como dos dobles clics reales deliberadamente rápidos). **Long-press** (500 ms quieto, anillo de aviso a los 200 ms en el overlay) = Alt+tap para aislar pieza: descarta el gesto abierto, **vacía la selección** (con el grupo ya seleccionado, la rama del marco combinado corre antes de que Alt pueda aislar) y re-ejecuta `onMouseDown` con `altKey: true`, dejando el dedo arrastrando la pieza. Los handles perdonan ×3 con `pointerType === 'touch'` (`hitHandle` acepta radio; el dibujo no cambia), `base/_touch.scss` solo añade mínimos bajo `(pointer: coarse)`, y `fitZoomToViewport` **sí baja del 100 % solo con pointer coarse** (excepción explícita a su invariante). En e2e el multitáctil se despacha por CDP (`Input.dispatchTouchEvent`) y `hasTouch: true` hace casar `(pointer: coarse)` en Chromium — con el panel-cajón cerrado `#zoom-val` no tiene `innerText`: leer el `value` del slider (ver `e2e/touch.spec.js`).

**Manipulación (v3.8.0).** Cuatro piezas: el **menú contextual** (`showContextMenu`, `#context-menu` — un `<div>`, NO un `<dialog>`, así que el guard de `dialog[open]` no lo cubre y el keydown lo atiende aparte: con él abierto, Escape cierra y ningún otro atajo toca el lienzo) — todo lo que ofrece existe como atajo o botón, jamás única vía; **Alt+arrastre duplica SOLO sobre la selección ya hecha** (`state.altDup`, armado en mousedown, consumado en el primer movimiento real — un Alt+clic sin arrastre no apila duplicados; `insertClones(…, 0, 0)` y el `dragSnapshot` del mousedown hacen de clonar+mover UN undo) — sobre una pieza sin seleccionar, Alt sigue aislándola: sacar una pieza de un edificio en un gesto no se toca; **copiar/pegar estilo** (`Ctrl+Alt+C`/`V` **por `e.code`**: Option+C escribe «ç» en macOS; `styledCopy` aplica cada campo solo donde significa algo — relleno a `FILLABLE_TYPES`, texto a `text`, discontinuo con el corte de `DASHABLE_TYPES`, nunca a un lápiz — y un campo ausente en el origen SE BORRA en el destino); y el **candado** (`locked: true`, patrón `ink`/`taper` en `isValidElement`, válido en cualquier tipo): `hitTest` lo salta (con `{lockedOnly: true}` es el modo del clic derecho para encontrarlo), la marquesina, `selectAll` y el borrador (`erase`/`doomedIndices` en eraser.js lo devuelven por referencia) lo ignoran, y **la única llave es el clic derecho** — `unlockAt` libera el grupo entero (una pieza suelta dejaría el edificio atascado) y el menú del lienzo ofrece «Desbloquear todo»: romper ese camino deja candados sin llave. Tool-lock se descartó: aquí la herramienta ya se queda puesta tras dibujar.

**Marcos y biblioteca de piezas (v3.12.0).** El `frame` **SÍ es un tipo de elemento** (no va en `CREATION_ONLY_TOOLS`) y cumple los 8 puntos de la checklist; lo propio suyo son tres reglas, cada una con guarda verificada contra su mutación:

- **`hitTest` lo coge por el BORDE (±6 px) o por su rótulo, jamás por dentro.** Con la caja entera, un marco de media pantalla se queda con todos los clics de su interior: ni se puede seleccionar lo que contiene ni tirar una marquesina ahí dentro. Misma idea que `OUTLINE_TYPES` en el borrador — lo que se ve, no la caja. (Y por eso está en `OUTLINE_TYPES`.)
- **Mover se lleva la carga; redimensionar no.** `frameCargo(sel)` devuelve lo que tiene el CENTRO dentro de la caja, excluyendo **otros marcos** (se solapan al componer: uno arrastraría medio dibujo), lo **bloqueado** y lo ya seleccionado (se movería dos veces). Se fija **una vez, en el mousedown** (`state.frameCargo`, muerto al soltar): calculada por fotograma, una pieza que saliera a mitad de arrastre se quedaría clavada. `moveSelectionBy` es el único consumidor, así que el nudge y los campos X/Y la recalculan solos. El recorte del lienzo sigue midiéndose sobre la selección, no sobre la carga.
- **Se dibuja recto** (sin Sketchy) y con `rough` borrado al crearlo: es una guía de trabajo, no un trazo. En HTML va por el `<svg>` incrustado (un `<div>` con borde sería un rectángulo más).
- **Un marco seleccionado exporta lo que ENMARCA**: `exportScope` devuelve la escena ENTERA con `box` = su caja exacta (sin `EXPORT_PAD`; su borde es el borde querido). Es para lo que existe un marco y salió gratis del recorte de la v3.9.0.

**La biblioteca** (`sketchwire.library`, tope 1 MB con aviso — la lección de la cuota) guarda piezas **normalizadas al origen** y sin `id`/anchors (una pieza es un dibujo, no un trozo de ESTA escena). Insertar reutiliza `withSeeds` + `insertClones`, que ya reasigna ids y un `buildingGroupId` nuevo por grupo. **Sobrevive a «Limpiar todo»** por estar en su propia clave: es del usuario, no del dibujo. La lista se construye con `createElement`/`textContent` (el nombre lo escribe el usuario y puede venir de un `.json` ajeno) y el borrado va en **dos pasos dentro de la fila**, porque la biblioteca no entra en el undo. `Exporter.downloadJSON`/`readJSONFile` son el par genérico para eso, al lado de `importJSON`, que sí impone forma de proyecto.

**Aspecto de boceto: trama, temblor y punta (v3.11.0).** Tres campos opcionales cuya **ausencia es el aspecto clásico** (`fillPattern`, `rough`, `headShape`), validados en `isValidElement` y transportados por `styledCopy`.

- **`src/js/hatch.js`** (módulo puro, cargado tras trapezoid.js) genera la trama donde se necesita —lienzo, SVG, HTML, miniaturas—, como el aerógrafo con sus gotas: el JSON no engorda y los cinco formatos no pueden divergir. Reglas: aleatoriedad por **hash sin estado por línea** (`_rnd(seed, k, canal)`), nunca una secuencia — una secuencia re-tira toda la trama al cambiar el número de líneas y la forma HIERVE al redimensionar; y la rejilla del barrido se ancla al **CENTRO** de la figura, que es lo único invariante bajo mover Y agrandar (anclarla al mínimo o al origen del lienzo la hace deslizar). `Hatch.outline(el)` es el MISMO contorno que dibuja el renderer (círculo muestreado, esquinas redondeadas reales, silueta de polígono/estrella), o la trama se saldría del dibujo. Cota de seguridad de 400 líneas / 20000 puntos. **`Renderer.hatchStyle(el)` NO es `fillStyle(el)`**: el relleno sin color propio cae al tinte 0x20 del trazo, que como superficie se lee y en rayas de un píxel es invisible. `_paintFill(ctx, el, path)` es el punto único de los seis tipos rellenables; en SVG la trama se emite **delante** del markup de la forma (en el lienzo el relleno va antes que el contorno) y la forma pierde su `fill` plano; en **HTML una escena con trama se desvía entera a `_svgScene`**, el mismo camino que los borradores.
- **El temblor entra por `Sketchy.setRoughness(factor)`**, junto a `setSeed`, y NO por las llamadas del renderer: son más de treinta y olvidar una haría temblar distinto a un elemento sin que nada fallara. `renderElement` lo fija por elemento y lo devuelve a 1 al terminar, o una forma «de caricatura» contagiaría a todo lo demás. Valores `ROUGH_VALUES = [0.5, 1, 2]` (1 = ausencia). **Solo cambia el lienzo y PNG/JPG**: el SVG y el HTML exportan geometría limpia desde siempre, y eso es una decisión, no un olvido (guardado).
- **`Sketchy.headGeometry(x,y,ang,len,shape)`** generaliza `arrowHead` (que se conserva) y la comparten renderer y exporter. `triangle` y `dot` son **macizos**: se rellenan con el color del trazo, no se trazan. `_paintHead` es el punto único de los cuatro extremos posibles (flecha y curva, punta y cola).
- Los tres mandos tienen semántica dual y **gemelos**: `#fill-pattern`/`#shape-modal-pattern` y `#stroke-modal-rough`/`#shape-modal-rough` (el temblor afecta a todo lo que dibuja Sketchy, así que tenerlo en un solo modal mentiría sobre su alcance); `#stroke-modal-head` se atenúa como la doble punta. Las opciones del HTML están pinneadas contra `Hatch.PATTERNS`/`Sketchy.HEAD_SHAPES` en smoke.

**Colocación: alinear, voltear y agrupar (v3.10.0).** `selectionUnits()` (app.js) es la pieza compartida: **la unidad es el GRUPO, no la pieza** —alinear elemento a elemento apila las ciento y pico de una fachada—, misma lección que `clampDelta`. `alignSelection(mode)` tiene ocho modos; distribuir deja quietos los extremos (repetirlo no desplaza el conjunto) y exige 3 unidades, alinear 2; por debajo es no-op **sin undo fantasma**. `mirrorSelection(axis)`/`mirrorAround(el, c, axis)` son calcados de `rotateSelection`/`rotateAround` salvo en tres puntos, y los tres importan: (1) **un solo régimen** —siempre alrededor del centro de la caja combinada, que con un elemento suelto es la suya—; (2) **la caja NO intercambia lados** (reflejar no es girar); (3) el giro de polígonos/estrellas/trapecio sigue una fórmula propia — voltear en horizontal (espejo sobre el eje vertical) manda φ→180−φ, o sea `rotation' = -rotation`, y en vertical φ→−φ, o sea `180 - rotation` —, exacta para cualquier ángulo, que conserva la paridad del índice (el radio exterior de las estrellas) y cae siempre en múltiplo del paso. **Copiar el giro de `rotateAround` no lo detecta ninguna guarda de ida-y-vuelta ni de validez**: hace falta comparar la SILUETA (`RegularPolygon.vertices`), que es lo que hace *"voltear un polígono GIRADO refleja su silueta"*. Un sólido `apex:'upright'` acumula `mirror: true` en su meta como ya hacía con `turns` (el gesto no se puede reflejar), con **orden fijo espejo→giros** en `regenerateSolid` y, por eso mismo, **añadir un espejo invierte los giros pendientes** (M·Rᵗ = R⁻ᵗ·M); un sólido clásico refleja su ángulo de fuga. `gardenMeta.p1/p2` y `clip` viajan reflejados. Una `image` se **repinta espejada** (`mirrorImage`, con la regla WebP/PNG de la v2.35.0) porque su dibujo es trama; el **texto no se refleja** (en espejo no se lee). `groupSelection`/`ungroupSelection` solo estampan y borran `buildingGroupId`: todo lo demás (clic al grupo, clonado, validación del id) ya existía — un grupo mezclado pierde el gid común que exigen `selectedGardenGroup`/`selectedSolid` y «Editar planta» desaparece hasta desagrupar, que es reversible con undo (no se guarda ningún `prevGroupId`). Atajos: `Ctrl+G`/`Ctrl+Mayús+G` por `e.code`, y **`Mayús+H`/`Mayús+V` OBLIGATORIAMENTE antes del bloque `TOOL_KEYS`**, que no filtra `shiftKey` y se llevaría la tecla; sin selección ceden y la tecla sigue eligiendo herramienta. `.panel__align` es la séptima vez de la trampa `[hidden]`.

**Ámbito y aspecto de la exportación (v3.9.0).** Los cinco formatos comparten un vocabulario de opciones que normaliza el propio exporter (`_cropBox`/`_exportScale`: una caja de anchura 0 o una escala de texto caen al valor de siempre, nunca producen un fichero roto): `box` (recorte en coordenadas de lienzo), `scale` 1–3 y `transparent`. Cada formato honra lo que puede representar y **eso es lo que hay que respetar al tocarlo**: el JPG compone el papel SIEMPRE (sin canal alfa saldría negro), el HTML ignora `scale` (es DOM, no raster) y el JSON ignora `scale`/`transparent` pero sí recorta. Cuatro trampas: el recorte se aplica con `ctx.setTransform` y el papel se compone DESPUÉS en espacio de dispositivo (`destination-over`), o el fondo seguiría la transformada del dibujo; `renderScene` acepta `background: null` (cualquier otro falsy sigue cayendo al blanco, así que ningún llamador anterior cambia); en el SVG el papel es un `<rect class="paper">` con la caja explícita, **nunca `width="100%"`** — con `viewBox` desplazado, el 100% se pinta en el origen y deja media exportación sin fondo, y la clase es además lo que permite distinguirlo de un rectángulo dibujado (las guardas buscaban `<rect x=`); y el recorte lleva `EXPORT_PAD` (8 px) porque el temblor de Sketchy se sale de la caja del elemento. `exportScope()` (app.js) es la fuente única del par {elementos, opciones} para los cinco botones y para el portapapeles. **Copiar al portapapeles** (`Exporter.copyImage`) va sin atajo a propósito (`Ctrl+Mayús+C` son las herramientas del navegador): sus vías son el botón del modal y el menú contextual; `canCopyImage()` decide si el botón está vivo — sin `ClipboardItem` (arnés vm incluido) devuelve false y nada promete lo que no puede cumplir. El blob se pasa como **promesa** dentro del `ClipboardItem` porque Safari exige construirlo en el mismo turno del gesto. `tests/helpers/dom-stub.js` estrena selectores de atributo (`[data-export]`) — sin ellos ese cableado no existía en el arnés.

**Moving a selection is clamped to the canvas (`clampDelta`/`moveSelectionBy`, v2.12.1).** `onMouseDown` calls `setPointerCapture`, so a drag stays alive far outside the canvas and `getPos` happily returns coordinates well past `CANVAS_W`/`CANVAS_H` — nothing related element coordinates to the fixed 1200×800 canvas, so flinging something off the edge *lost* it: still counted in «Elementos» and still travelling inside exported JSON, but invisible and unreachable (a click can't land out there and a marquee can only be drawn over the canvas, so even select-all didn't bring it back). Every path that moves a selection now goes through `moveSelectionBy` — the drag, the arrow-key nudge and the X/Y fields of «Posición y tamaño» — keeping `KEEP_VISIBLE` (24) px of the box on canvas, or the whole object when it is smaller. Four properties to preserve, each with a guard:

- **The clamp applies to the selection's combined box, never piece by piece** — clamping each element separately would dismantle a building against the edge instead of stopping it as one thing.
- **It only limits movement that makes things worse.** An element already outside (a project saved before this version) must still be draggable back in, and moving a selection that contains one must not yank it to the edge and break its position relative to the rest.
- **`dragLast` stores the pointer's real position, not the clamped one**, so when the pointer comes back the object follows from the first pixel instead of having to re-cross the distance it overshot.
- **`applyGeometry` clamps before its no-op guard**, so a typed X of 9000 resyncs the field to where the element actually ended up rather than promising a position it doesn't have.

### Conectores anclados: la flecha de dentro NO es un conector (v3.14.2)

Soltar un extremo de flecha sobre un anclable (`ANCHORABLE_TYPES`, que incluye
`image`) lo conecta, y `resolveAnchors` lo materializa en cada repintado sobre
el **perímetro** del bbox con `rectEdgePoint` — que prolonga el rayo hasta el
borde aunque el punto venga de dentro. Con un elemento que cubre el lienzo (una
imagen encuadrada, y `rasterErase` fabrica `type:'image'` a partir de cualquier
cosa que el borrador parta) eso mandaba el origen de la flecha al borde del
lienzo mientras el cuerpo se quedaba donde se trazó: el bug de la v3.14.2.

**La regla:** `connectorAnchorTarget(p, other, excludeIdx)` es el único camino a
`findAnchorTarget` en creación y en resize, y devuelve -1 cuando los **dos**
extremos caen sobre el mismo anclable. Una flecha dibujada entera encima de
algo es una anotación, no un conector. Dos cosas que no se deducen del código:

- **La guarda de «no anclar los dos extremos al mismo elemento» no bastaba**:
  al aplicarse solo al segundo extremo dejaba `startAnchor` sin `endAnchor`, y
  esa asimetría esquiva justo la comprobación equivalente de `resolveAnchors`.
- **La decisión va en `resizeTo`, no en el `onMouseUp`**, porque `anchorCandidate`
  es además lo que pinta el resaltado turquesa: decidir al soltar dejaba el
  feedback prometiendo un anclaje que ya no iba a ocurrir.

Guardas en `tests/app-interaction.test.js` (tres que fallan sin el arreglo, más
una que ata el conector de siempre, el que sale del elemento hacia fuera) y en
`e2e/imagen-flecha.spec.js`, que lo repite con un PNG real soltado en el lienzo
— excepción razonada a la regla de reparto: son coordenadas, pero el arnés
`node:vm` no decodifica imágenes y el fallo se destapó justo con una.

### «Limpiar todo» y los valores de fábrica

**`appDefaults()` (app.js) es la fuente ÚNICA de los ajustes de fábrica**, y la
usan las dos cosas que ponen el estado entero de golpe: el `state` inicial y el
botón «Limpiar todo». Un ajuste nuevo se resetea solo por estar ahí. Hasta la
v2.22.1 el botón mantenía **su propia lista escrita a mano**, y se había quedado
atrás en dieciséis ajustes (color, grosor, tamaño de letra, el relleno entero,
discontinuo, doble punta, cuadrícula, ajustar a la rejilla, clics acumulativos,
la letra del lienzo y los tres del estilo de texto). Cuatro cosas que conviene
no volver a separar:

- **Es una función, no un objeto.** `uiLabels` se muta en sitio
  (`state.uiLabels[tool] = …`), así que compartir la referencia haría que el
  botón devolviera lo último escrito en vez del valor de fábrica. Cualquier
  campo anidado que se añada hereda esa protección sin pensarlo.
- **Los ajustes que se persisten tienen que volver EN `state`, no solo en
  `localStorage`.** El botón borra la clave, pero si el valor sigue vivo en
  `state` el primer `savePrefs()` posterior —cambiar el color del fondo, sin ir
  más lejos— lo reescribe entero y reaparece en la recarga siguiente.
- **`syncAllControls()` es el punto único que vuelca `state` en los mandos**, y
  lo llaman el arranque y el botón. Los de semántica dual (color, grosor,
  relleno, tamaño de letra, discontinuo, doble punta) no hacen falta ahí:
  `redrawNow` los sincroniza en cada repintado y sin selección enseña justo
  esos defaults. Los que no tienen quien los sincronice, sí — y por eso el
  tamaño del borrador estrena `syncEraserControls()`: su mando solo lo escribían
  `applyEraserSize` y `openEraserSizeModal`, así que el estado se reseteaba y el
  deslizador seguía enseñando el tamaño anterior.
- **La herramienta vuelve al Lápiz con `silent`.** Aquí nadie ha pulsado una
  herramienta, así que abrirle sus ajustes encima del lienzo recién vaciado
  sería un modal que nadie pidió — el mismo motivo por el que las cuatro
  activaciones automáticas de Mover pasan ese flag.

**El ASPECTO del lienzo es la excepción deliberada (v3.0.0): papel, color de
rejilla y `showGrid` sobreviven al borrado.** No describen el dibujo sino la
mesa de trabajo, y el camino de vuelta a «Pizarra» o «Blanco» son dos códigos de
color que no aparecen en ninguna parte de la interfaz: resetearlos obligaba a
recomponer el aspecto a mano después de cada limpieza. Se conservan copiando los
tres campos **antes** del `Object.assign` y reponiéndolos detrás de
`appDefaults()`, y se **reescriben en prefs en el acto** (`savePrefs()` justo
después del `removeItem`) en vez de confiar en que otro mando guarde más tarde
— si no, el aspecto conservado moriría en la siguiente recarga. Guardado por
*«Limpiar todo» conserva el aspecto del lienzo, y lo deja guardado*, y la guarda
grande exceptúa esos tres mandos **exigiendo** que sobrevivan, para que nadie
los devuelva a fábrica sin enterarse.

Lo único que además **no** vuelve a un valor fijo es el zoom, que re-ejecuta el
auto-ajuste (`zoomManual = false` + `fitZoomToViewport()`, la pareja que corre
`init()`): forzar el 100 % encogía el lienzo y dejaba el auto-ajuste apagado el
resto de la sesión (ver `BUGS.md`). Guardado por *«Limpiar todo» devuelve TODOS
los ajustes a los de fábrica, no solo unos cuantos*, que compara treinta mandos
contra un arranque limpio.

### Canvas background / grid color

**Detalle completo: `docs/lienzo-aspecto.md`.** `state.canvasBg`/`gridColor`/`showGrid` son prefs cosméticas (`sketchwire.prefs`, sin undo); «Limpiar todo» las **conserva** (v3.0.0) y re-ejecuta el auto-ajuste de zoom. `CANVAS_PRESETS` (v2.31.0, cinco entradas): un clic fija los tres campos a la vez; un preset **nunca toca la tinta** ni entra en undo; **el primero ES el estado de fábrica** (pinneado contra `DEFAULT_CANVAS_BG`/`DEFAULT_GRID_COLOR`/`appDefaults()`); la muestra dibuja la rejilla (es lo único que distingue «Blanco» de «Milimetrado»); si nada coincide, nada se marca. Defaults desde v2.20.0: papel `#686f92`, rejilla `#fcfcfc`. **Ninguna exportación de DIBUJO los lleva** — PNG/JPG componen blanco propio, el SVG `fill="white"`, el HTML `background: #fff` — pero **el JSON sí** desde v3.1.0 (`settings.canvasBg`/`gridColor`/`showGrid`, solo si válidos; el import los expone como propiedades no enumerables, y **su ausencia significa «no tocar el aspecto», nunca resetear**). `Renderer.drawGrid` conserva su default `#cdd3de` (asume papel blanco) y no debe seguir al de la app. El color del papel está escrito dos veces (app.js y el `value` de `#canvas-bg-picker` en index.html) y smoke compara los dos ficheros. `paintedPixels` (e2e/helpers.js) mide la tinta **relativa al papel** (60% de su luminancia), no bajo un umbral fijo.

